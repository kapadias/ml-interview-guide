{
  "questions": [
    {
      "volume": "DL",
      "volume_name": "Deep Learning",
      "chapter": 1,
      "chapter_title": "Mathematical Foundations",
      "level": "L5",
      "type": "First Principles",
      "question": "Why does the chain rule matter for backpropagation? Walk through a 3-layer example.",
      "answer_source": "Backpropagation **is** the chain rule applied systematically through a computational graph. Consider a 3-layer network: $\\vh_1 = \\sigma(\\mW_1 \\vx)$, $\\vh_2 = \\sigma(\\mW_2 \\vh_1)$, $\\hat{y} = \\mW_3 \\vh_2$, with loss $\\loss$.\n\nTo compute $\\frac{\\partial \\loss}{\\partial \\mW_1}$, the chain rule gives:\n$$\\frac{\\partial \\loss}{\\partial \\mW_1} = \\frac{\\partial \\loss}{\\partial \\hat{y}} \\cdot \\frac{\\partial \\hat{y}}{\\partial \\vh_2} \\cdot \\frac{\\partial \\vh_2}{\\partial \\vh_1} \\cdot \\frac{\\partial \\vh_1}{\\partial \\mW_1}$$\n\nEach factor is a Jacobian matrix. The backward pass computes this right-to-left, reusing intermediate results. The gradient at layer $\\ell$ flows through all layers above it—this is why deep networks face vanishing/exploding gradients: the product of many Jacobians either shrinks or grows exponentially.\n\nKey efficiency insight: **reverse-mode** autodiff (backprop) computes gradients for all parameters in one backward pass (cost $\\sim 2\\times$ forward), while **forward-mode** would require one pass per parameter (prohibitive for millions of parameters). This is because reverse mode propagates a scalar (loss) backward, while forward mode propagates a perturbation forward.",
      "red_flags_source": "- Describing backprop as \"something different from the chain rule\" (it is literally the chain rule)\n \n- Not knowing why reverse mode is preferred over forward mode for neural networks\n \n- Forgetting that activations must be stored for the backward pass (memory cost of backprop)",
      "followups_source": "\"What is gradient checkpointing?\" (Trade compute for memory: recompute activations during backward pass instead of storing them. Reduces activation memory from $O(L)$ to $O(\\sqrt{L})$.) \"When would you use forward-mode autodiff?\" (When you have few inputs and many outputs, e.g., computing a full Jacobian row.)",
      "status": "recut",
      "id": 2,
      "say": "Backpropagation isn't like the chain rule, it is the chain rule, applied systematically over a computational graph. Take a three-layer net: h1 is a nonlinearity of W1 x, h2 the same on W2 h1, and the output is W3 h2. The gradient of the loss with respect to W1 is a product of four factors - loss to output, output to h2, h2 to h1, h1 to W1. Each one is a Jacobian, and the backward pass evaluates that product from the loss end, reusing intermediates as it goes.\n\nTwo things fall out of that. First, the gradient at any layer is a product of all the Jacobians above it, so if those factors run consistently under one you get vanishing gradients and over one you get exploding gradients. That product is the entire reason residual connections and normalization exist.\n\nSecond, why reverse mode. The loss is a scalar, so propagating backward from it gets gradients for every parameter in one pass, at roughly twice the cost of a forward pass. Forward mode propagates a perturbation from the inputs, so you'd need one pass per parameter - hopeless for billions of weights. The price is memory: you keep the forward activations around to evaluate those Jacobians, which is exactly what gradient checkpointing trades back for compute.",
      "numbers": [
        "Backward pass costs roughly 2x the forward pass",
        "Gradient checkpointing: activation memory O(L) -> O(sqrt(L))"
      ],
      "red_flags": [
        "Describes backprop as a separate algorithm from the chain rule rather than an application of it.",
        "Can't say why reverse mode beats forward mode when there are millions of parameters.",
        "Forgets that the backward pass needs the stored forward activations, so misses the memory cost."
      ],
      "followups": [
        "What does gradient checkpointing buy you, and what does it cost?",
        "When would forward-mode autodiff actually be the right choice?",
        "Where in that Jacobian product do vanishing gradients come from?"
      ],
      "topic": "Foundations",
      "whiteboard": "$\\frac{\\partial \\mathcal{L}}{\\partial W_1} = \\frac{\\partial \\mathcal{L}}{\\partial \\hat{y}}\\cdot\\frac{\\partial \\hat{y}}{\\partial h_2}\\cdot\\frac{\\partial h_2}{\\partial h_1}\\cdot\\frac{\\partial h_1}{\\partial W_1}$ Write the four factors out, then say the backward pass evaluates them starting from the loss factor on the left, accumulating a vector-Jacobian product at each step, which is why one pass gets every parameter. Point at the two middle Jacobians as the ones whose product vanishes or explodes."
    },
    {
      "volume": "DL",
      "volume_name": "Deep Learning",
      "chapter": 2,
      "chapter_title": "Learning Theory Essentials",
      "level": "L5",
      "type": "Conceptual",
      "question": "Explain double descent. Why does test error decrease again in the over-parameterized regime?",
      "answer_source": "Double descent describes how test error behaves as model capacity increases, revealing three distinct regimes that contradict the classical U-shaped bias-variance curve:\n\n \n- **Under-parameterized** ($p < n$): Classical regime. Increasing parameters reduces bias faster than it increases variance, so test error decreases. Eventually variance dominates and test error rises—the familiar U-curve.\n \n- **Interpolation threshold** ($p \\approx n$): The model has just enough capacity to fit training data exactly, but is forced into a unique, fragile solution with maximum variance. This is the worst-case generalization point.\n \n- **Over-parameterized** ($p \\gg n$): Many weight configurations achieve zero training loss. Gradient descent with standard initialization (small random weights) converges to the minimum-norm interpolating solution, which tends to be smoother and generalizes better. This is **implicit regularization**—the optimizer, not explicit regularization, selects good solutions.\n\nThe key insight is geometric: at the interpolation threshold, the solution is unique and often high-norm (jagged). With more parameters, the solution space becomes a high-dimensional manifold, and SGD navigates to a smooth, low-norm point on that manifold.\n\nDouble descent also occurs along the **epoch axis**: test error can peak mid-training, then decrease again with further training, even after the model has memorized the data.",
      "red_flags_source": "- Saying \"more parameters always means more overfitting\" (the classical view that double descent refutes)\n \n- Not being able to explain **why** over-parameterized models generalize (implicit regularization, minimum-norm solutions)\n \n- Confusing interpolation threshold with overfitting (overfitting is gradual; the threshold is a specific capacity point)",
      "followups_source": "\"Does double descent happen with epochs too?\" (Yes—epoch-wise double descent is observed.) \"Does regularization eliminate the peak?\" (It can reduce or shift it, but does not eliminate the phenomenon.) \"How does this relate to scaling laws?\" (Scaling laws describe the over-parameterized regime where more capacity reliably improves performance.)",
      "status": "recut",
      "id": 7,
      "say": "Double descent is the observation that test error as a function of model capacity isn't the classical U. It goes down, up, and then down again, and that second descent is the regime every modern model lives in.\n\nThe first part is familiar: adding parameters cuts bias faster than it raises variance, until variance takes over and error climbs. The peak sits right at the interpolation threshold, where the model has just barely enough capacity to fit the training set exactly. That's the worst place to be, because there's essentially one interpolating solution and you're forced into it however jagged it is.\n\nPast that point you keep adding capacity and error falls again. Now many parameter settings achieve zero training loss, so the question becomes which one the optimizer picks - and from small random initialization, gradient descent converges to something close to the minimum-norm interpolator, which is smoother and generalizes better. That's implicit regularization: the optimizer, not a penalty term, selects the good solution.\n\nIt also shows up along the epoch axis - test error can peak partway through training and come down again well after the model has memorized the data. So \"more parameters means more overfitting\" just isn't a reliable rule.",
      "numbers": [],
      "red_flags": [
        "\"More parameters always means more overfitting\" - that is exactly the claim double descent refutes.",
        "Describes the second descent without explaining it: no implicit regularization, no minimum-norm story.",
        "Treats the interpolation threshold as ordinary overfitting rather than a specific capacity point."
      ],
      "followups": [
        "Does explicit regularization remove the peak?",
        "Does this happen along the epoch axis too?",
        "How does this square with scaling laws?"
      ],
      "topic": "Foundations"
    },
    {
      "volume": "DL",
      "volume_name": "Deep Learning",
      "chapter": 3,
      "chapter_title": "Loss Functions: The Interview Playbook",
      "level": "L5",
      "type": "Trade-off",
      "question": "Cross-entropy vs. focal loss vs. class-balanced loss for imbalanced classification - what's your decision framework?",
      "answer_source": "The choice depends on the **degree of imbalance**, whether you care more about **hard examples vs.\\ rare classes**, and your **tolerance for hyperparameter tuning**.\n\n**Standard cross-entropy** works when imbalance is mild ($<$5:1 ratio). It implicitly weights by class frequency (majority class contributes more gradients), but the model can still learn minority classes if there are enough examples.\n\n**Class-weighted cross-entropy** ($\\loss = -w_c \\log \\hat{y}_c$) directly addresses class frequency imbalance by upweighting rare classes. Weights can be inverse frequency ($w_c = N/N_c$), inverse square root ($w_c = \\sqrt{N/N_c}$), or the effective number formulation ($w_c = (1-\\beta)/(1-\\beta^{N_c})$). This is the simplest intervention: one extra hyperparameter (or none, if using inverse frequency). However, it treats all examples within a class equally—it does not distinguish hard from easy examples.\n\n**Focal loss** ($\\loss = -\\alpha_t(1-p_t)^\\gamma \\log p_t$) addresses a different problem: the **dominance of easy examples** in the gradient. The $(1-p_t)^\\gamma$ factor down-weights examples the model already classifies correctly with high confidence. This is critical when the majority class is not just more frequent but also trivially easy (e.g., background in object detection, where 99.9\\\n\n**Decision framework:**\n\\begin{table}[H]\n\\centering\n\\caption{Imbalanced classification loss decision framework}\n\\begin{tabularx}{\\textwidth}{lXX}\n\\toprule\n**Scenario** & **Recommended** & **Why** \n\\midrule\nMild imbalance ($<$5:1) & Cross-entropy & Simple, sufficient \nModerate imbalance (5:1–100:1) & Weighted cross-entropy & Addresses frequency directly \nExtreme imbalance ($>$100:1) & Focal loss & Easy examples dominate gradient \nExtreme + long-tail classes & Focal + effective number weights & Addresses both frequency and difficulty \nMulti-label with rare positives & Asymmetric loss & Per-label imbalance \n\\bottomrule\n\\end{tabularx}\n\\end{table}\n\n**Important nuance:** Class weighting and focal loss are **orthogonal**. Weighting addresses how much each **class** contributes; focal loss addresses how much each **example** (by difficulty) contributes. They can and often should be combined.",
      "red_flags_source": "- \"Always use focal loss for imbalanced data\" — focal loss is designed for easy-example dominance, not all forms of imbalance. For moderate imbalance without easy-example dominance, weighted CE is simpler and equally effective.\n \n- \"Just oversample the minority class\" — oversampling is a data-level intervention, not a loss-level intervention; they are complementary, and the question specifically asks about loss functions.",
      "followups_source": "\"How does focal loss affect model calibration?\" \"What about oversampling or data augmentation—when is that better than loss modification?\" \"How do you choose $\\gamma$ for focal loss?\"",
      "status": "recut",
      "id": 12,
      "say": "I pick based on two separate questions: is the problem class frequency, or is it easy-example dominance? Those are different failures with different fixes.\n\nIf imbalance is mild, say under five to one, plain cross-entropy is usually fine. The minority class still gets enough gradient to learn. Once you're in the five to one through hundred to one range, I reach for class-weighted cross-entropy first: upweight rare classes by inverse frequency, inverse square root, or the effective-number weights if the tail is long. One hyperparameter, and it attacks the thing that's actually wrong.\n\nFocal loss solves something else. The one-minus-p-to-the-gamma factor down-weights examples the model already gets right with high confidence, so it's for the case where the majority class isn't just frequent but trivially easy. The canonical one is object detection, where 99.9 percent of anchors are easy background and the gradient signal drowns in them. If your negatives are genuinely hard, focal buys you little and costs you a gamma to tune plus some calibration damage.\n\nThe point most candidates miss is that these are orthogonal. Weighting controls how much each class contributes, focal controls how much each example contributes by difficulty, and at extreme long-tail imbalance you usually want both: focal loss with effective-number alpha weights.",
      "numbers": [
        "Object detection: ~99.9% of anchors are easy background",
        "Rough bands: under 5:1 plain CE; 5:1-100:1 weighted CE; beyond 100:1 focal"
      ],
      "red_flags": [
        "\"Always use focal loss for imbalanced data\" - it targets easy-example dominance, not imbalance in general.",
        "Answers with resampling when the question was about the loss; related, but not what was asked.",
        "Treats class weighting and focal loss as alternatives rather than composable."
      ],
      "followups": [
        "What does focal loss do to calibration?",
        "How do you choose gamma?",
        "When is oversampling the better lever than changing the loss?"
      ],
      "topic": "Training"
    },
    {
      "volume": "DL",
      "volume_name": "Deep Learning",
      "chapter": 4,
      "chapter_title": "Activation Functions and Normalization",
      "level": "L5",
      "type": "Conceptual",
      "question": "Explain the dying ReLU problem. How do modern activations solve it?",
      "answer_source": "**The dying ReLU problem:** ReLU outputs zero for all negative inputs: $\\text{ReLU}(x) = \\max(0,x)$. Critically, its gradient is also zero for $x \\leq 0$. If a neuron's pre-activation is negative for **every** sample in the dataset, it receives zero gradient and its weights never update—the neuron is permanently \"dead.\"\n\n**How neurons die:** (1) A large gradient update pushes the bias to a very negative value, shifting the pre-activation distribution below zero. (2) Poor initialization (e.g., large negative biases) starts neurons in the dead zone. (3) High learning rates amplify both effects. Once dead, the neuron can never recover because it receives exactly zero gradient—there is no mechanism to \"revive\" it. In deep networks, this can cascade: dead neurons in layer $l$ reduce the expressivity available to layer $l+1$, potentially killing more neurons.\n\n**Diagnosis:** Compute the fraction of zero activations per layer. If $>$30–40\\\n\n**Modern solutions, from simplest to most complex:**\n\n**Leaky ReLU / PReLU:** $f(x) = \\max(\\alpha x, x)$ with $\\alpha = 0.01$ (Leaky) or learned (PReLU). The small negative slope ensures a nonzero gradient for $x < 0$, so neurons can always receive updates. Simple, effective, but the negative slope is somewhat arbitrary.\n\n**ELU:** $f(x) = x$ for $x > 0$, $\\alpha(e^x - 1)$ for $x \\leq 0$. Smooth at zero, and the negative saturation pushes mean activations closer to zero. But the exponential is more expensive than ReLU.\n\n**GELU:** $f(x) = x \\cdot \\Phi(x)$. Smoothly gates the input by its Gaussian percentile. For large negative inputs, the output and gradient are small but nonzero. For values near zero, the transition is smooth, avoiding the gradient discontinuity. This \"soft thresholding\" is the key conceptual advance: instead of a hard zero/pass decision, GELU makes a probabilistic decision.\n\n**SiLU/Swish:** $f(x) = x \\cdot \\sigma(x)$. Self-gated: the sigmoid of $x$ gates $x$ itself. Has a small negative region (minimum $\\approx -0.28$ at $x \\approx -1.28$), which empirically acts as regularization. Used in EfficientNet and as part of SwiGLU.\n\nAll modern activations solve dying ReLU through the same principle: ensuring **nonzero gradients for negative inputs**. The evolution is from simple fixes (Leaky ReLU) to smooth, expressive functions (GELU, SiLU) that additionally provide better optimization landscapes.",
      "red_flags_source": "- \"Dying ReLU happens when the output is zero\"—the problem is that the **gradient** is zero, preventing recovery.\n \n- \"Just use a smaller learning rate\"—this reduces the probability but doesn't eliminate the fundamental issue.\n \n- Not knowing that GELU still allows near-zero activations for negative inputs; the key is that the gradient is nonzero.",
      "followups_source": "\"How does He initialization specifically address dying ReLU?\" \"Can a dead ReLU neuron ever recover?\" \"What is the relationship between activation sparsity and model quality?\"",
      "status": "recut",
      "id": 17,
      "say": "A ReLU unit is dead when its pre-activation is negative for every example in the dataset. The output being zero isn't the problem; the problem is that the gradient is also exactly zero there, so the weights never update and nothing revives it. It's a one-way door.\n\nNeurons get there from a large update driving the bias very negative, from a bad initialization, or from a learning rate high enough to do that in one step. You diagnose it by hooking activations and logging the fraction of zeros per layer - if thirty to forty percent of units are zero across the whole dataset, not just per batch, you have it.\n\nEvery modern activation fixes it the same way: guarantee a nonzero gradient on the negative side. Leaky ReLU and PReLU do it bluntly with a small negative slope. ELU does it smoothly and pulls mean activations toward zero, but you pay for the exponential. GELU multiplies the input by its Gaussian CDF, so instead of a hard on-off decision you get a soft gate, with small but nonzero gradient for negative inputs. SiLU is the same idea self-gated by a sigmoid, dipping slightly negative around minus one point three, and it's the one inside SwiGLU, which is what most current LLMs ship.",
      "numbers": [
        "Alarm threshold: >30-40% zero activations per layer across the dataset",
        "SiLU minimum ~ -0.28 at x ~ -1.28"
      ],
      "red_flags": [
        "Says the problem is that the output is zero; the problem is the zero gradient, which removes any path to recovery.",
        "\"Just lower the learning rate\" - reduces the odds, doesn't remove the mechanism.",
        "Thinks GELU works by never being near zero; the point is the gradient is nonzero, not the activation."
      ],
      "followups": [
        "Can a dead ReLU unit ever come back?",
        "How does He initialization help here?",
        "Is activation sparsity always bad?"
      ],
      "topic": "Architectures"
    },
    {
      "volume": "DL",
      "volume_name": "Deep Learning",
      "chapter": 5,
      "chapter_title": "Attention Mechanisms and Transformers",
      "level": "L5",
      "type": "Conceptual",
      "question": "Explain Flash Attention. Why is it faster even though it does the same computation?",
      "answer_source": "Flash Attention computes **exactly** the same result as standard attention, but reorganizes the computation to minimize memory I/O:\n\n \n- **Problem**: Standard attention materializes the full $n \\times n$ matrix in slow HBM memory (40 GB on A100 at $\\sim$2 TB/s; 80 GB HBM3 on H100 at $\\sim$3.35 TB/s). The bottleneck is reading/writing this matrix, not computing it—attention is memory-bound, not compute-bound.\n \n- **Solution**: Process attention in tiles that fit in fast SRAM (20 MB on A100 at $\\sim$19 TB/s; H100's SRAM is similarly tiny relative to HBM). Use the online softmax trick to compute exact softmax incrementally without ever materializing the full matrix. Each tile computes a partial softmax, maintaining running max and sum statistics that get corrected as new tiles arrive.\n \n- **Result**: Memory drops from $O(n^2)$ to $O(n)$. Speed improves 2–4$\\times$ because operations become compute-bound rather than memory-bound. Enables longer sequences that would otherwise OOM.",
      "red_flags_source": "- \"Flash Attention is an approximation\" — it computes the **exact** same result\n \n- \"It reduces the number of FLOPs\" — the FLOPs are identical; the speedup comes from reduced memory I/O\n \n- Not knowing about SRAM vs HBM distinction",
      "followups_source": "\"What's the online softmax trick?\" \"When would Flash Attention NOT help?\" (Small sequences where attention isn't the bottleneck; naive short-context decode where the bottleneck is weight loading—though Flash-Decoding's split-KV parallelism does help long-context decode.) \"What changed in Flash Attention 2 vs 1? And 3 vs 2?\" (FA2: better work partitioning across thread blocks, reduced non-matmul FLOPs. FA3: Hopper-native—warp-specialized producer/consumer pipelines with TMA async copies, softmax-matmul overlap, FP8 support.)",
      "status": "recut",
      "id": 22,
      "say": "Flash Attention is exact - same result as standard attention, same FLOPs. It just stops moving the attention matrix through slow memory. Attention at typical sequence lengths is memory-bound, not compute-bound, so what you optimize is I/O, not arithmetic.\n\nStandard attention materializes the full n-by-n score matrix in HBM: write it, read it back for the softmax, write again, read again for the value multiply. On an H100 that's eighty gigabytes of HBM at roughly 3.35 terabytes a second against about twenty megabytes of on-chip SRAM an order of magnitude faster. The kernel spends its life on round trips while the tensor cores sit idle.\n\nFlash Attention tiles the computation so a block of queries and keys fits in SRAM, and uses the online softmax trick - carry a running max and running sum, rescale as new tiles arrive - to get the exact softmax without ever materializing the full matrix. Memory drops from quadratic in sequence length to linear, and attention runs two to four times faster because it becomes compute-bound.\n\nIf they push on versions: FA2 improved work partitioning across thread blocks and cut non-matmul FLOPs; FA3 is Hopper-native, with warp specialization, async copies and FP8.",
      "numbers": [
        "H100: 80 GB HBM at ~3.35 TB/s vs ~20 MB SRAM",
        "Memory O(n^2) -> O(n); attention ~2-4x faster"
      ],
      "red_flags": [
        "Calls it an approximation - it is bit-for-bit the same computation.",
        "Says it reduces FLOPs; the FLOPs are identical, the memory traffic is what changes.",
        "No mental model of SRAM versus HBM, so can't explain why identical math runs faster."
      ],
      "followups": [
        "What is the online softmax trick doing?",
        "When does Flash Attention not help?",
        "What changed in FA2, and in FA3?"
      ],
      "topic": "Architectures",
      "notes": "Corrected a source error: the source paired \"40 GB on A100\" with \"~2 TB/s\". 2 TB/s is the 80 GB A100; the 40 GB part is ~1.55 TB/s. Dropped the A100 figures and quoted only the H100 numbers, which the source states correctly."
    },
    {
      "volume": "DL",
      "volume_name": "Deep Learning",
      "chapter": 5,
      "chapter_title": "Attention Mechanisms and Transformers",
      "level": "L6",
      "type": "Debugging",
      "question": "Your large BF16 pretraining run shows intermittent loss spikes. Instrumentation shows attention logits growing into the hundreds over training. Diagnose and fix.",
      "answer_source": "This is **attention-logit growth**, a canonical large-scale instability. The $1/\\sqrt{d_k}$ scaling only guarantees unit-variance logits at **initialization**; during training the norms of $\\mW^Q$ and $\\mW^K$ (and of the residual stream) grow, so $\\vq^T\\vk$ logits can grow without bound. The failure chain: large logits $\\to$ softmax saturates to one-hot (attention entropy collapse) $\\to$ tiny gradients through most of the softmax with occasional huge ones $\\to$ loss spikes. BF16 makes it worse not by overflowing (its exponent range matches FP32) but through its 8-bit mantissa: at magnitude $\\sim$256 the spacing between representable values exceeds 1, so nearby logits become indistinguishable and $\\exp(\\cdot)$ can still overflow if the softmax is computed without max-subtraction.\n\n**Diagnosis**: (1) log the **max attention logit** per layer per head over training—the standard early-warning metric (ViT-22B; Wortsman et al.'s small-scale proxies work); a slow upward trend into the 50–100+ range predicts spikes before they happen. (2) Correlate spike steps with which layer's logits blew up. (3) Confirm numerics hygiene: softmax with max-subtraction, FP32 accumulation inside attention (FlashAttention does both by default).\n\n**The mitigation ladder** (in order of modern preference):\n\n \n- **QK-norm**: RMSNorm applied to queries and keys before the dot product—bounds logits by construction, negligible cost, composes with FlashAttention. The 2025-era default (Qwen3, Gemma-3); see the normalization chapter (Chapter~) and its NaN-loss debugging question for the general instability workflow.\n \n- **Logit soft-capping**: $z \\leftarrow c \\cdot \\tanh(z/c)$ (Gemma-2, $c{=}50$). Works, but breaks stock fused attention kernels and was dropped by Gemma-3 in favor of QK-norm.\n \n- **Training-side levers** if you cannot change the architecture mid-run: lower peak LR / extend warmup, tighter gradient clipping, and z-loss on the output softmax for the sibling divergence at the LM head.",
      "red_flags_source": "- \"BF16 has FP32's range, so nothing can overflow\" — misses precision collapse at large magnitudes and $\\exp(\\cdot)$ overflow without max-subtraction.\n \n- \"Just lower the learning rate\" — symptom-level fix; the logit growth resumes and you've paid a convergence tax.\n \n- Not knowing QK-norm exists, or proposing to fix it purely with more aggressive gradient clipping.",
      "followups_source": "\"Why doesn't the $1/\\sqrt{d_k}$ scaling prevent this?\" (It normalizes only under init-time variance assumptions; learned correlations grow.) \"Where exactly does QK-norm go, and what does it cost?\" \"Your run is 40\\",
      "status": "recut",
      "id": 27,
      "say": "That's attention-logit growth, and the fix is QK-norm. The one-over-root-d-k scaling only buys unit-variance logits at initialization; during training the query and key projection norms grow, the residual stream grows, and nothing bounds the dot product. Once logits reach the hundreds the softmax saturates to one-hot, attention entropy collapses, and you get long stretches of near-zero gradient punctuated by huge ones. That's the spikes.\n\nBF16 makes it worse, but not by overflowing - its exponent range matches FP32. It's precision: around magnitude 256 the gap between representable BF16 values exceeds one, so logits that should be distinct collapse together, and exp can still overflow if softmax is computed without max subtraction.\n\nFor diagnosis I'd log the max attention logit per layer per head - the standard early-warning signal since ViT-22B, where a slow climb into the fifty-to-a-hundred range predicts spikes before they land. Correlate spike steps with which layer went first, and confirm numerics hygiene: max-subtracted softmax, FP32 accumulation inside attention, both of which FlashAttention does by default.\n\nThen the ladder. QK-norm - RMSNorm on queries and keys before the dot product - bounds logits by construction, costs almost nothing, composes with fused kernels, and it's what Qwen3 and Gemma-3 do. Tanh soft-capping works, Gemma-2 capped at fifty, but it breaks stock attention kernels, which is why Gemma-3 dropped it. If you can't change architecture mid-run: lower peak LR, longer warmup, tighter clipping, z-loss at the output softmax.",
      "numbers": [
        "Max attention logit drifting into the 50-100+ range is the early warning",
        "BF16: spacing between representable values exceeds 1 around magnitude 256"
      ],
      "red_flags": [
        "\"BF16 has FP32's range so nothing can overflow\" - misses precision collapse and exp overflow without max subtraction.",
        "\"Lower the learning rate\" as the fix - the growth resumes and you've paid a convergence tax.",
        "Doesn't know QK-norm exists, or wants to solve it entirely with more aggressive gradient clipping."
      ],
      "followups": [
        "Why doesn't the 1/sqrt(d_k) scaling prevent this?",
        "Where exactly does QK-norm go, and what does it cost?",
        "You're 40% through the run and spiking - restart with QK-norm, or patch with LR and clipping?"
      ],
      "topic": "Training"
    },
    {
      "volume": "DL",
      "volume_name": "Deep Learning",
      "chapter": 6,
      "chapter_title": "Embeddings and Representation Learning",
      "level": "L5",
      "type": "Conceptual",
      "question": "How does subword tokenization (BPE) interact with embedding quality?",
      "answer_source": "Subword tokenization (BPE, WordPiece, SentencePiece) determines the granularity at which embeddings are learned, and this has direct consequences for embedding quality.\n\n**Frequency-quality correlation.** Frequent words get single tokens (\"the,\" \"is\") and therefore have dedicated embedding vectors trained on millions of examples. Rare words get split into multiple subword tokens (\"photosynthesis\" $\\to$ `[photo, synth, esis]`). Each subword has its own embedding, but the model must **compose** them through subsequent layers (attention, FFN) to reconstruct the word's meaning. This composition is lossy—the model has seen fewer training examples of this specific combination, so the composed representation is noisier.\n\n**Vocabulary size trade-off.** Larger vocabularies (128K in LLaMA 3 vs. 50K in GPT-2 or 32K in LLaMA 2) mean more words get single-token representations. Benefits: better per-token semantics, shorter sequences (fewer tokens per text $\\to$ fits more context in the window, faster inference). Costs: larger embedding table (more parameters, more memory), each token seen less frequently during training (sparser signal). The optimal vocabulary size depends on the training data size—more data can support a larger vocabulary.\n\n**Embedding quality impacts by token type:**\n\n \n- **Common words** (single token): High-quality embeddings—many training examples, clear semantic signal.\n \n- **Subword fragments** (\"un,\" \"ing,\" \"tion\"): Embeddings capture morphological/syntactic patterns rather than standalone semantics. Quality depends on how consistently the fragment is used across contexts.\n \n- **Rare/domain-specific words** (multi-token): Quality depends on the composition mechanism. Transformers handle this better than RNNs because attention can directly connect all subword tokens.\n \n- **Non-English and code tokens**: Often heavily fragmented in English-centric tokenizers, leading to poor per-token embeddings and longer sequences. Multilingual tokenizers (SentencePiece trained on diverse data) mitigate this.\n\n**Practical implications for embedding-based retrieval:** (1) If your domain has specialized vocabulary (medical terms, product names, code identifiers), the tokenizer may fragment these heavily, degrading retrieval quality. Fine-tuning on domain data helps because the model learns to compose these fragments correctly. (2) For sentence embeddings, the pooling strategy interacts with tokenization—mean pooling over many fragment tokens dilutes the semantic signal compared to pooling over fewer whole-word tokens.",
      "red_flags_source": "- \"BPE is just about handling OOV words\"—it fundamentally shapes the embedding space structure.\n \n- \"Larger vocabulary is always better\"—there's a data-dependent optimal size.\n \n- Not knowing that BPE tokenization affects non-English languages disproportionately (more fragmentation $\\to$ longer sequences $\\to$ worse quality).",
      "followups_source": "\"How would you choose vocabulary size for a new language model?\" \"How does tokenization affect multilingual models?\" \"What are Matryoshka embeddings and how do they address the dimension trade-off?\"",
      "status": "recut",
      "id": 32,
      "say": "Tokenization sets the granularity at which embeddings are learned, so it decides which words get a trained vector of their own and which have to be reassembled from pieces. Frequent words get a single token and an embedding updated millions of times. Rare words get split - photosynthesis becomes three or four fragments - and the model composes their meaning through attention and the feedforward layers. That works, but it's noisier, because it has seen that particular combination far less often.\n\nSo vocabulary size is a real trade-off, not a free parameter. Llama 3 went to 128K from Llama 2's 32K, and GPT-2 was 50K. Bigger vocabulary means more words get their own token, better per-token semantics, shorter sequences, more text per context window, cheaper inference. The cost is a much larger embedding table and each token seen less often in training, so the right size depends on how much data you have.\n\nWhere it bites is domain and language. English-centric tokenizers shred other languages and code identifiers into fragments, so you get worse per-token embeddings and longer sequences at once. For retrieval over medical terms or product SKUs, expect fragmentation to hurt, and expect domain fine-tuning to help mainly because the model learns to compose those fragments correctly.",
      "numbers": [
        "Vocabulary sizes: GPT-2 50K, Llama 2 32K, Llama 3 128K"
      ],
      "red_flags": [
        "\"BPE is just OOV handling\" - it shapes the structure of the whole embedding space.",
        "\"Bigger vocabulary is always better\" - the optimum depends on training data size.",
        "Doesn't know English-centric tokenizers disproportionately fragment other languages and code."
      ],
      "followups": [
        "How would you pick a vocabulary size for a new language model?",
        "What does tokenization do to a multilingual model?",
        "What problem are Matryoshka embeddings solving?"
      ],
      "topic": "NLP & LLMs"
    },
    {
      "volume": "DL",
      "volume_name": "Deep Learning",
      "chapter": 7,
      "chapter_title": "Similarity and Metric Learning Architectures",
      "level": "L5",
      "type": "Conceptual",
      "question": "Cosine similarity vs dot product vs L2 distance for embeddings—when each?",
      "answer_source": "These three metrics are closely related but differ in whether they account for embedding magnitude, which has practical implications.\n\n**Cosine similarity**: $\\cos(\\vu, \\vv) = \\frac{\\vu^T\\vv}{\\|\\vu\\|\\|\\vv\\|}$. Measures the angle between vectors, ignoring magnitude. Range: $[-1, 1]$. **Use when:** (1) You want purely directional similarity (semantic similarity regardless of \"confidence\" or frequency). (2) Embeddings have variable norms (e.g., frequent items have larger norms due to more gradient updates). (3) Combining embeddings from different models or scales. **Practical note:** If you L2-normalize all embeddings to unit norm at indexing time, cosine similarity equals dot product, and you can use the faster dot product at query time.\n\n**Dot product**: $\\vu^T\\vv = \\|\\vu\\|\\|\\vv\\|\\cos(\\vu,\\vv)$. Measures both angular similarity and magnitude. Range: $(-\\infty, \\infty)$. **Use when:** (1) Magnitude carries useful information—e.g., the norm of a document embedding correlates with document quality or relevance, and you want the retrieval score to reflect this. (2) MIPS (Maximum Inner Product Search) is your retrieval primitive—most ANN libraries are optimized for this. (3) Embeddings are already normalized (then dot product = cosine). **Example:** In recommendation systems, item embedding norms often correlate with popularity. Dot product naturally boosts popular items, which may be desirable.\n\n**L2 (Euclidean) distance**: $\\|\\vu - \\vv\\|_2 = \\sqrt{\\sum_i(u_i-v_i)^2}$. Measures geometric distance. Range: $[0, \\infty)$. **Use when:** (1) You need a proper distance metric (triangle inequality holds, useful for clustering algorithms). (2) The embedding space was trained with L2-based losses (triplet loss with Euclidean distance, contrastive loss with distance). (3) You're doing k-means clustering on embeddings. **Relationship:** For unit-normalized vectors, $\\|\\vu - \\vv\\|_2^2 = 2(1 - \\cos(\\vu,\\vv))$, so L2 distance is a monotonic function of cosine similarity. In this case, all three metrics produce the same nearest-neighbor ranking.\n\n**Key insight for interviews:** The choice matters most when embeddings are **not** normalized. With L2-normalized embeddings, all three metrics produce identical rankings, so the choice is purely about computational convenience (dot product is fastest for ANN search).\n\n**When the choice has real impact:**\n\n \n- **Unnormalized embeddings**: Dot product favors high-norm vectors (popular/frequent items). Cosine ignores this. L2 penalizes large-norm differences. Choose based on whether popularity bias is desired.\n \n- **Multi-modal embeddings**: CLIP normalizes embeddings to the unit sphere, so all metrics are equivalent. But if combining CLIP with other features (price, recency), dot product on unnormalized concatenated vectors lets different feature groups contribute different magnitudes.\n \n- **ANN index compatibility**: HNSW natively supports L2 and inner product. Faiss supports all three. Choose based on your infrastructure.",
      "red_flags_source": "- \"Cosine is always best for embeddings\"—it depends on whether magnitude carries useful information.\n \n- Not knowing the equivalence for normalized vectors—this is the most commonly tested fact.\n \n- \"L2 distance and cosine similarity are unrelated\"—they are monotonically related for unit vectors.",
      "followups_source": "\"How does the choice of similarity metric interact with ANN algorithms?\" \"What is MIPS and why is it the standard retrieval primitive?\" \"How would you handle a case where you want cosine for ranking but dot product for speed?\"",
      "status": "recut",
      "id": 37,
      "say": "For L2-normalized embeddings all three give you the same ranking, so the choice is about computation, not semantics. Cosine is the dot product after normalization, and squared L2 between unit vectors is two times one minus cosine - a monotone transform. So normalize at index time and use dot product, because that's what maximum inner product search and every ANN library is built for.\n\nThe choice only matters when embeddings aren't normalized, and then it's a question about magnitude. Dot product rewards large norms, and norms correlate with frequency and popularity, because those items got more gradient updates. In a recommender that's often a feature - dot product naturally boosts popular items. In semantic search it's usually a bug, and cosine strips it out.\n\nL2 I reach for when I need an actual metric with the triangle inequality, so k-means or clustering on the embeddings, or when the space was trained with a distance-based loss like triplet or contrastive with Euclidean margins. Match the retrieval metric to the training objective - that's the rule that actually matters.\n\nCLIP and most modern encoders already normalize, so the question is moot there. It comes straight back the moment you concatenate unnormalized side features like price or recency.",
      "numbers": [
        "For unit vectors: ||u - v||^2 = 2(1 - cos(u,v)), so all three rank identically"
      ],
      "red_flags": [
        "\"Cosine is always right for embeddings\" - only if magnitude carries nothing you want.",
        "Doesn't know the three are rank-equivalent for normalized vectors; that's the most commonly tested fact here.",
        "Picks a metric without asking what loss the embeddings were trained with."
      ],
      "followups": [
        "What is MIPS, and why is it the standard retrieval primitive?",
        "How does the metric choice interact with your ANN index?",
        "You want cosine semantics but dot-product speed - what do you do?"
      ],
      "topic": "Retrieval & Ranking"
    },
    {
      "volume": "DL",
      "volume_name": "Deep Learning",
      "chapter": 9,
      "chapter_title": "NLP Architectures",
      "level": "L5",
      "type": "Estimation",
      "question": "You're told a model is \"Llama-class, 7B parameters.\" Roughly where do those 7B parameters live?",
      "answer_source": "Start from the per-layer budget. A decoder block is attention plus FFN: attention is four $d \\times d$ projections $= 4d^2$; the FFN is $\\approx 8d^2$ (classic $4d$ hidden, or SwiGLU's three matrices at $d_{ff} \\approx \\tfrac{8}{3}d$). So $\\approx 12d^2$ per layer. Llama-7B has $d = 4096$ and $L = 32$: $12 \\cdot 32 \\cdot 4096^2 \\approx 6.4$B. Embeddings add $2Vd = 2 \\cdot 32000 \\cdot 4096 \\approx 0.26$B (untied input embedding + LM head), giving $\\approx 6.7$B—the actual model is 6.74B.\n\nThe split: $\\sim$64\\\n\n**Adjustments an interviewer may probe:** GQA trims only the attention slice ($4d^2 \\to \\sim 2.5d^2$; roughly 10\\",
      "red_flags_source": "- Assuming attention dominates—the FFN is $2\\times$ larger\n \n- Forgetting the LM head / not knowing whether embeddings are tied\n \n- Unable to explain why $d_{ff} = 11008$ rather than $4d = 16384$ (SwiGLU's three-matrix budget)\n \n- Needing the config handed over—the point of the question is reconstructing it",
      "followups_source": "\"How does the split change for Mixtral 8$\\times$7B—derive the 47B.\" \"Why is $d_{ff} \\approx 2.7d$ in Llama?\" \"At what model size does a 256K vocabulary become the dominant cost?\"",
      "status": "recut",
      "id": 42,
      "say": "Roughly two thirds in the feedforward layers, about a third in attention, and a few percent in embeddings. I'd reconstruct that rather than recall it.\n\nPer decoder block, attention is four d-by-d projections, so four d squared. The feedforward is about eight d squared, either the classic four-d hidden with two matrices, or SwiGLU's three matrices at about eight-thirds d, which lands in the same place. Call it twelve d squared per layer. Llama-7B is d equals 4096 across 32 layers, so twelve times thirty-two times 4096 squared is about 6.4 billion. Embeddings add two times vocab times d - 32,000 by 4096, twice because the input embedding and the LM head are untied - which is another 0.26 billion. That gives 6.7, and the real model is 6.74.\n\nSo about 64 percent feedforward, 32 percent attention, 4 percent embeddings. Worth saying unprompted: feedforward dominance is exactly why mixture-of-experts replaces the FFN and not attention, and why pruning and quantization work concentrates there.\n\nThings that move it: grouped-query attention shrinks only the attention slice, from four d squared to roughly two and a half - about ten percent of the block, and its real payoff is KV cache, not parameters. A 128K vocabulary pushes embeddings toward a billion, material at 8B and dominant below 1B. Tied embeddings halve that term. For MoE the FFN term replicates per expert for total parameters, but only the routed experts count as active.",
      "numbers": [
        "Llama-7B: d=4096, L=32, V=32000 -> 12*d^2*L ~ 6.4B plus ~0.26B embeddings = 6.74B",
        "Split: ~64% FFN, ~32% attention, ~4% embeddings"
      ],
      "red_flags": [
        "Assumes attention holds most of the parameters; the FFN slice is twice as large.",
        "Forgets the LM head, or can't say whether embeddings are tied.",
        "Asks for the config to be handed over - reconstructing it is the whole exercise."
      ],
      "followups": [
        "Do the same for Mixtral 8x7B - where does 47B come from?",
        "Why is d_ff 11008 in Llama rather than 4d = 16384?",
        "At what model size does a 256K vocabulary dominate the budget?"
      ],
      "topic": "Architectures"
    },
    {
      "volume": "DL",
      "volume_name": "Deep Learning",
      "chapter": 9,
      "chapter_title": "NLP Architectures",
      "level": "L6",
      "type": "First Principles",
      "question": "Walk me through the DPO loss function. How does it eliminate the need for a separate reward model?",
      "answer_source": "DPO (Direct Preference Optimization) is an alignment technique that trains directly on preference pairs without needing to train a reward model or run RL. Starting from the RLHF objective (maximize reward while staying close to reference policy), the key insight is that the optimal policy has a closed-form relationship to the reward function.\n\nIn RLHF, the optimal policy satisfies: $\\pi^*(y|x) = \\frac{1}{Z(x)} \\pi_{\\text{ref}}(y|x) \\exp\\left(\\frac{r(x,y)}{\\beta}\\right)$. Rearranging to solve for $r$: $r(x,y) = \\beta \\log \\frac{\\pi^*(y|x)}{\\pi_{\\text{ref}}(y|x)} + \\beta \\log Z(x)$.\n\nSubstituting this into the Bradley-Terry preference model $p(y_w \\succ y_l | x) = \\sigma(r(x,y_w) - r(x,y_l))$, the $Z(x)$ terms cancel:\n\n$$\\loss_{\\text{DPO}} = -\\E_{(x, y_w, y_l)}\\left[\\log \\sigma\\left(\\beta \\log \\frac{\\pi_\\theta(y_w|x)}{\\pi_{\\text{ref}}(y_w|x)} - \\beta \\log \\frac{\\pi_\\theta(y_l|x)}{\\pi_{\\text{ref}}(y_l|x)}\\right)\\right]$$\n\n**Intuition:** DPO increases the log-probability ratio (vs.\\ reference) for preferred outputs and decreases it for dispreferred outputs. The $\\beta$ parameter controls how far the policy can drift from the reference. The implicit reward is the log-probability ratio itself.\n\n**Advantages over RLHF:** (1) No reward model to train (saves a training stage). (2) No RL instability (PPO is notoriously hard to tune). (3) Simpler implementation (just supervised fine-tuning with a special loss). (4) Often similar or better results in practice.\n\n**Limitations:** (1) Requires good preference data (garbage in, garbage out). (2) The reference model must be reasonably good (DPO refines, doesn't teach from scratch). (3) Can overfit to the preference dataset distribution. (4) Doesn't easily support online data collection (preference pairs are fixed).",
      "red_flags_source": "- Not knowing the derivation from RLHF objective\n \n- Saying DPO doesn't use a reward model at all—it has an **implicit** reward model\n \n- Claiming DPO always beats RLHF",
      "followups_source": "\"What is $\\beta$ doing intuitively?\" \"When would you still prefer RLHF over DPO?\" \"What is KTO and how does it differ from DPO?\"",
      "status": "recut",
      "id": 47,
      "say": "DPO trains on preference pairs with a plain supervised loss, and it works because the reward model was never strictly necessary - you can solve for it in closed form.\n\nThe argument: the RLHF objective is maximize reward minus a KL penalty against a reference policy, and that has a known optimal solution, the reference reweighted by the exponential of reward over beta with a normalizing partition function. Invert it and reward becomes beta times the log ratio of policy to reference, plus a term depending only on the prompt. Push that into the Bradley-Terry preference model and the prompt-only term is identical on both sides, so it cancels - partition function and all. What's left is a logistic loss on the difference of log ratios between the chosen and the rejected response, trained exactly like supervised fine-tuning.\n\nSo the reward model isn't gone, it's implicit: the log-probability ratio to the reference is the reward, and beta controls how far you're allowed to drift from it.\n\nWhat you gain is one fewer training stage and no PPO to tune. What you give up is a fixed offline preference set with no online exploration, and DPO refines a decent reference model rather than teaching from scratch. Thin preference data or a need for online collection, and RLHF still earns its cost.",
      "numbers": [],
      "red_flags": [
        "Says DPO has no reward model at all; it has an implicit one, the log ratio to the reference.",
        "Can't get from the RLHF objective to the loss, so can't say why the partition function cancels.",
        "Claims DPO always beats RLHF."
      ],
      "followups": [
        "What is beta doing intuitively?",
        "When would you still run full RLHF?",
        "What is KTO, and how does it differ?"
      ],
      "topic": "Alignment & Safety"
    },
    {
      "volume": "DL",
      "volume_name": "Deep Learning",
      "chapter": 11,
      "chapter_title": "Generative Models",
      "level": "L5",
      "type": "Conceptual",
      "question": "What is classifier-free guidance and why does it improve generation quality in diffusion models?",
      "answer_source": "Classifier-free guidance (CFG) is a technique that improves the quality and condition-adherence of conditional diffusion models by amplifying the effect of the conditioning signal during sampling.\n\n**Mechanism.** During training, the model learns both conditional and unconditional denoising: randomly drop the conditioning (text prompt) with probability $p$ (typically 10–20\\\n$$\\tilde{\\epsilon}_\\theta(x_t, c) = \\epsilon_\\theta(x_t, \\emptyset) + w \\cdot (\\epsilon_\\theta(x_t, c) - \\epsilon_\\theta(x_t, \\emptyset))$$\nwhere $w$ is the guidance scale (typically 5–15). The term $(\\epsilon_\\theta(x_t, c) - \\epsilon_\\theta(x_t, \\emptyset))$ is the \"direction\" that the conditioning pushes the prediction. Multiplying by $w > 1$ amplifies this direction.\n\n**Intuition.** Think of it as implicit classifier guidance. Ho & Salimans (2022) showed that the CFG update is equivalent to using an implicit classifier $p(c|x_t) \\propto p(x_t|c)/p(x_t)$. The guidance scale $w$ controls how strongly the model follows this implicit classifier. Higher $w$ means stronger conditioning adherence—images match the prompt more closely but lose diversity.\n\n**Why it works.** Without guidance ($w = 1$), diffusion models produce diverse but sometimes off-topic samples. The conditional distribution $p(x|c)$ is broad. CFG sharpens this distribution, producing samples closer to the mode of $p(x|c)$. This trades diversity for quality and relevance—exactly the tradeoff users want for text-to-image generation.\n\n**Guidance scale tradeoff.** $w = 1$: no guidance (pure conditional sampling). $w = 3$–$7$: balanced quality and diversity. $w = 7$–$15$: strong prompt adherence, reduced diversity. $w > 20$: oversaturation, artifacts, loss of coherence. Stable Diffusion defaults to $w = 7.5$.\n\n**Why not classifier guidance?** Earlier work (Dhariwal & Nichol, 2021) used a separately trained classifier to guide diffusion. CFG is superior because: (1) no extra classifier needed, (2) works with any conditioning (text, image, class label), (3) the conditioning comes from the same model, ensuring coherence.",
      "red_flags_source": "- Confusing classifier-free guidance with classifier guidance\n \n- Not knowing that it requires training with random conditioning dropout\n \n- Saying \"higher guidance is always better\"—there's a quality-diversity tradeoff",
      "followups_source": "\"How do negative prompts work with CFG?\" \"What guidance scale would you use for photorealism vs.\\ artistic generation?\" \"How does CFG interact with the number of diffusion steps?\"",
      "status": "recut",
      "id": 52,
      "say": "Classifier-free guidance amplifies the conditioning at sampling time by extrapolating away from the unconditional prediction. During training you randomly drop the text condition, typically ten to twenty percent of the time, so the same network learns both conditional and unconditional denoising. At sampling you run it both ways and take the unconditional prediction plus w times the difference between the two. That difference is the direction the prompt is pushing; scaling it above one pushes further than the model would go on its own.\n\nWhy it helps: the conditional distribution the model learned is broad, so plain conditional sampling gives diverse but often off-prompt images, and guidance sharpens toward the modes. Ho and Salimans showed the update is equivalent to guiding with an implicit classifier built from the ratio of conditional to unconditional density, which is why you don't need to train a separate one - and that's the difference from classifier guidance, which did.\n\nIt's a knob, not free quality. Three to seven is balanced, seven to fifteen gives strong adherence with less diversity, Stable Diffusion defaults to 7.5, and past twenty you get oversaturation and artifacts. So CFG trades diversity for prompt adherence, which for text-to-image happens to be the trade users want.",
      "numbers": [
        "Condition dropout during training: ~10-20%",
        "Guidance scale: 3-7 balanced, 7-15 strong adherence, Stable Diffusion default 7.5"
      ],
      "red_flags": [
        "Confuses it with classifier guidance, which needs a separately trained noisy-image classifier.",
        "Doesn't know it requires condition dropout during training; it isn't a pure inference-time trick.",
        "\"Higher guidance is always better\" - past the sweet spot you get oversaturation and lose diversity."
      ],
      "followups": [
        "How do negative prompts fit into this?",
        "What scale would you use for photorealism versus stylized work?",
        "How does CFG interact with the number of sampling steps?"
      ],
      "topic": "Architectures"
    },
    {
      "volume": "DL",
      "volume_name": "Deep Learning",
      "chapter": 14,
      "chapter_title": "Efficient Architectures",
      "level": "L6",
      "type": "Trade-off",
      "question": "INT8 vs INT4 vs FP8 quantization—what is your decision framework?",
      "answer_source": "The choice depends on three factors: target hardware, acceptable accuracy loss, and whether you are quantizing for inference or training.\n\n\\begin{table}[H]\n\\centering\n\\caption{Quantization format decision matrix}\n\\begin{tabularx}{\\textwidth}{lXXX}\n\\toprule\n**Property** & **INT8** & **INT4** & **FP8 (E4M3)** \n\\midrule\nMemory reduction & $2\\times$ (vs.\\ FP16) & $4\\times$ (vs.\\ FP16) & $2\\times$ (vs.\\ FP16) \nAccuracy loss & Near-zero ($<$0.5\\\nHardware support & Broad (A100, V100, CPU) & Limited (needs dequant kernels) & H100+ only \nCalibration effort & Minimal (SmoothQuant) & Moderate (GPTQ/AWQ, hours) & Minimal (per-tensor scaling) \nBest method & SmoothQuant, W8A8 & GPTQ, AWQ, GGUF & Native FP8 (torch.float8) \n\\bottomrule\n\\end{tabularx}\n\\end{table}\n\n**INT8** is the default for inference. SmoothQuant enables both weight and activation quantization (W8A8) by migrating quantization difficulty from activations (which have outlier channels) to weights (which are easier to quantize). Near-zero accuracy loss on most models. Use it on any hardware that supports INT8 tensor cores.\n\n**INT4** is for memory-constrained deployment. GPTQ uses approximate second-order information (Hessian) to quantize weights layer-by-layer, adjusting remaining weights to compensate for quantization error. AWQ is faster—it identifies and protects the $\\sim$1\\\n\n**FP8** is the new default for H100+ inference (and increasingly training). E4M3 format (4 exponent, 3 mantissa bits) provides better dynamic range than INT8 for the same 8-bit budget. Per-tensor dynamic scaling handles outliers without the need for SmoothQuant-style preprocessing. Native hardware support means minimal kernel engineering.\n\n**Decision framework:**\n\n \n- H100+ hardware $\\to$ FP8 (E4M3) by default—simplest and fastest\n \n- A100/older GPU $\\to$ INT8 (SmoothQuant) by default\n \n- Need to fit a larger model on fewer GPUs $\\to$ INT4 (AWQ or GPTQ)\n \n- CPU inference (llama.cpp) $\\to$ GGUF with mixed precision (important layers at Q5/Q6, others at Q4)\n \n- Training $\\to$ FP8 on H100+ for $\\sim$$2\\times$ throughput; otherwise BF16",
      "red_flags_source": "- \"INT4 is always better because it uses less memory\"—INT4 has meaningful accuracy degradation, especially on reasoning tasks. The right precision depends on your accuracy budget.\n \n- \"FP8 and INT8 are the same thing in different formats\"—FP8 has floating-point dynamic range (can represent very small and very large values); INT8 has fixed range determined by the scale/zero-point. FP8 handles activation outliers more naturally.\n \n- \"Quantization doesn't affect model quality\"—it does, especially at INT4 and below. Always benchmark on your specific task, not just perplexity.",
      "followups_source": "\"How does SmoothQuant handle activation outliers?\" \"Why does INT4 degrade math and code more than general text?\" \"Can you combine INT4 weights with INT8 activations?\" \"What is the GGUF format and why does it matter for CPU inference?\"",
      "status": "recut",
      "id": 57,
      "say": "I choose on hardware first, then accuracy budget. On Hopper and newer, FP8 in E4M3 is the default: native tensor-core support, per-tensor dynamic scaling, half the memory of FP16, near-zero quality loss, almost no kernel engineering. FP8 beats INT8 at the same bit width because of dynamic range - floating point absorbs activation outliers, where INT8's fixed scale and zero point get stretched by them.\n\nOn A100-class or older hardware, or on CPU, INT8 is the answer, and the technique is SmoothQuant. It migrates quantization difficulty out of activations, which have nasty outlier channels, into weights, which are well behaved, and that's what makes W8A8 work at all. Expect under half a percent perplexity change.\n\nINT4 is for when you must fit a bigger model on fewer GPUs and you'll pay for it. GPTQ quantizes layer by layer using approximate second-order information, compensating with the weights it hasn't done yet; AWQ is faster and protects the roughly one percent of weight channels that see large activations. Both give usable models, but degradation lands on reasoning, math and code rather than general text, so benchmark on your task, not perplexity.\n\nTwo places this has moved: FP8 is no longer H100-only, since Ada-generation cards have it too, and Blackwell adds native FP4, which makes four-bit a first-class hardware path.",
      "numbers": [
        "INT8: <0.5% perplexity loss; INT4: ~1-3%",
        "AWQ protects the ~1% of weight channels with the largest activations"
      ],
      "red_flags": [
        "\"INT4 always, it uses least memory\" - the degradation lands on reasoning and code, and it needs a stated accuracy budget.",
        "\"FP8 and INT8 are the same eight bits\" - FP8 has floating-point dynamic range, which is the whole point for activations.",
        "Validates only on perplexity instead of the task the model actually does."
      ],
      "followups": [
        "How does SmoothQuant move difficulty from activations into weights?",
        "Why does INT4 hurt math and code more than general text?",
        "Can you combine INT4 weights with INT8 activations?"
      ],
      "topic": "Inference & Serving",
      "notes": "Corrected a stale claim: the source says FP8 is \"H100+ only.\" Ada-generation GPUs (L4/L40S, RTX 40-series) also have FP8 tensor cores, and Blackwell adds native FP4, which changes the INT4 calculus the source frames as needing dequant kernels. Both added in one closing sentence."
    },
    {
      "volume": "DL",
      "volume_name": "Deep Learning",
      "chapter": 15,
      "chapter_title": "Training Optimization",
      "level": "L7",
      "type": "Debugging",
      "question": "A node dies every 3 hours on your 16K-GPU run. Design the recovery system.",
      "answer_source": "First, establish that this failure rate is **expected**, not a broken cluster: 16{,}384 GPUs at $\\sim$50K-hour per-GPU MTBF gives $\\approx 0.33$ failures/hour—one every 3 hours is exactly the Llama-3 405B experience (419 unexpected interruptions in 54 days on 16K H100s). So the design goal is not \"stop failures\" but \"make each failure cost minutes, automatically.\" I would budget the system around four components:\n\n**1. Checkpoint cadence—do the math.** Cluster MTBF $M = 3$ h $= 10{,}800$ s. With a naive synchronous checkpoint costing $\\delta = 60$ s of stalled training, the Young/Daly optimum is $\\tau \\approx \\sqrt{2\\delta M} = \\sqrt{2 \\times 60 \\times 10{,}800} \\approx 19$ min. Expected waste per failure $\\approx \\tau/2 \\approx 9.5$ min of lost work, plus $\\sim$5\\\n\n**2. Make checkpoints nearly free.** Two standard steps: (a) **sharded** checkpoints—each rank writes its own parameter/optimizer shard (ZeRO/FSDP already hold state sharded), so write bandwidth scales with the cluster; (b) **asynchronous** checkpoints—snapshot to host memory in seconds, resume training, persist to durable storage in the background. That drives the training-visible $\\delta$ to seconds, which by the same formula justifies checkpointing every few minutes; per-failure loss drops to a couple of minutes of work. The end state is torchft-style in-memory recovery: checkpoint replicas live in peer host/GPU memory across DP replica groups, and a failed group restores from a live peer with no storage round-trip, while the other groups continue.\n\n**3. Automate detect–quarantine–replace with hot spares.** Recovery latency is dominated by detection and re-provisioning, so: health watchdogs (NCCL timeout tuned down from the default, heartbeats, ECC/Xid monitors) detect within seconds; the scheduler drains and quarantines the node; a **hot spare**—warm, image-loaded, network-configured; I would reserve $\\sim$1–2\\\n\n**4. Handle the failures that don't kill the job.** Two classes are nastier than clean deaths: **stragglers**—a throttling GPU makes every synchronous step slow; detect via per-rank step-time and collective-wait profiling, evict ranks consistently last into the all-reduce—and **silent data corruption**: NaN/Inf watchdogs on loss and grad norms with automatic rewind, periodic redundant recomputation to catch a GPU computing wrong values, and a replay-on-different-hardware protocol before blaming the optimizer (ties into the loss-spike playbook).\n\n**Sanity check the budget:** 8 failures/day $\\times$ ($\\sim$2 min lost work + $\\sim$3 min restore) $\\approx$ 40 GPU-cluster-minutes/day $\\approx$ 3\\",
      "red_flags_source": "- \"Checkpoint every hour\" (or any cadence not derived from MTBF and checkpoint cost)—the interviewer is testing exactly this arithmetic.\n \n- Treating the failure rate as abnormal (\"first, fix the hardware\")—at 16K GPUs this rate **is** healthy hardware.\n \n- No automation story: a design where a human notices the failure has already lost the game—at one failure per 3 hours, on-call humans are the bottleneck and the error source.\n \n- Ignoring stragglers and SDC—the failures that don't throw errors cost more than the ones that do.",
      "followups_source": "\"How big should the hot-spare pool be for a 4-week run?\" \"What changes if checkpoint storage bandwidth is the bottleneck?\" \"How does in-memory checkpointing interact with HSDP replica groups?\" \"How do you detect a GPU that computes wrong values without crashing?\"",
      "status": "recut",
      "id": 62,
      "say": "One failure every three hours at 16K GPUs is healthy hardware, not a broken cluster. Sixteen thousand GPUs at roughly a 50,000-hour per-GPU MTBF is about a third of a failure per hour - exactly the Llama-3 405B experience, 419 unexpected interruptions in 54 days. So the goal isn't fewer failures, it's making each one cost minutes with nobody paged.\n\nI'd derive the checkpoint cadence, not guess it. Three-hour MTBF against a sixty-second synchronous checkpoint gives a Young-Daly optimum near nineteen minutes, so about ten minutes of lost work per failure. But the square root says the real lever is shrinking that sixty seconds: sharded writes, each rank persisting its own shard since FSDP already holds state that way, plus async checkpointing - snapshot to host memory, resume, flush in the background. Visible cost drops to seconds, so you checkpoint every few minutes. The end state is in-memory replicas across data-parallel groups, restoring from a live peer with no storage round trip.\n\nThen automate detect, quarantine, replace. NCCL timeouts tuned down, heartbeats and ECC monitors catching it in seconds, the scheduler draining the node, and a hot spare - one or two percent of the fleet kept warm - spliced in. Node death to training resumed in single-digit minutes, no human.\n\nAnd budget for the failures that don't crash: stragglers, caught by per-rank step-time profiling, and silent data corruption, caught by NaN watchdogs and periodic redundant recompute. Those cost more than the clean deaths.",
      "numbers": [
        "16,384 GPUs at ~50K-hour per-GPU MTBF ~ 0.33 failures/hour",
        "Young-Daly: interval ~ sqrt(2 * delta * MTBF) ~ 19 min at delta=60s, MTBF=3h",
        "Llama-3 405B: 419 unexpected interruptions in 54 days on 16K H100s"
      ],
      "red_flags": [
        "Names a checkpoint interval without deriving it from MTBF and write cost; that arithmetic is the question.",
        "Treats the failure rate as abnormal and starts with \"first, fix the hardware.\"",
        "No automation story - at eight failures a day, any design where a human notices first has already lost."
      ],
      "followups": [
        "How big a hot-spare pool would you keep for a four-week run?",
        "What changes if checkpoint storage bandwidth is the bottleneck?",
        "How do you detect a GPU computing wrong values without crashing?"
      ],
      "topic": "Training"
    },
    {
      "volume": "DL",
      "volume_name": "Deep Learning",
      "chapter": 16,
      "chapter_title": "Transfer Learning and Parameter-Efficient Fine-Tuning",
      "level": "L6",
      "type": "Debugging",
      "question": "Your fine-tuned model echoes the prompt before answering—why?",
      "answer_source": "The most likely cause is **missing loss masking on prompt tokens**. If the training loop set `labels = input_ids` without masking the prompt span to the ignore index ($-100$), the model was trained to predict prompt tokens as well as response tokens—and since prompts often dominate token counts, most of the gradient signal taught it to generate user-turn text. The trained behavior is exactly the symptom: given a prompt, produce more prompt-shaped text (echo or paraphrase it), then answer, and often invent a fresh user turn afterward. Verification takes minutes: decode one collated batch from the actual training pipeline and print which positions carry loss (`labels != -100`); if prompt positions carry loss, that is the bug. Fix: a completion-only collator (TRL's `DataCollatorForCompletionOnlyLM`, Axolotl's `train_on_inputs: false`) and retrain—cheap, since LoRA SFT at typical scale is an hours-long single-GPU job. Two differentials to rule out before retraining. First, **template mismatch**: if serving renders a different chat template than training—or omits the generation prompt—the model may be **completing the user's turn** rather than starting the assistant's; check by rendering the exact inference-time token sequence and diffing it against a training example. Second, **base-model residue**: a non-instruct base checkpoint fine-tuned on only a few hundred examples can echo as leftover pretraining behavior (documents that restate questions); the fix there is more or better SFT data, not the mask. The discriminating test: a masking bug produces echoing even when you replay a literal training example through the training-time template offline; a template mismatch disappears under that replay.",
      "red_flags_source": "- Immediately blaming the base model or \"not enough data\" without inspecting a collated training batch\n \n- Not knowing the labels/ignore-index mechanics at all\n \n- Proposing to hide it—a system prompt telling the model not to echo, or string-stripping the echoed prompt in postprocessing (an acceptable stopgap, but it leaves the model wasting tokens and signals you cannot find the root cause)",
      "followups_source": "\"You enabled sequence packing and quality dropped—what happened?\" (cross-example attention without block-diagonal masks; position IDs not reset at boundaries) \"The model never stops generating—what do you check?\" (EOS present at the end of assistant turns, not excluded by the loss mask, registered as a special token, and the same template at train and serve time)",
      "status": "recut",
      "id": 67,
      "say": "Almost always it's missing loss masking on the prompt tokens. If the training loop set labels equal to input_ids without setting the prompt span to the ignore index, you trained the model to predict prompt tokens as well as response tokens - and since prompts usually outnumber responses, most of your gradient taught it to produce user-turn text. The symptom is exactly what that asks for: restate the prompt, answer, and often invent a fresh user turn afterwards.\n\nVerifying takes ten minutes. Pull one collated batch out of the real training pipeline, decode it, and print which positions carry loss. If prompt positions do, that's the bug. The fix is a completion-only collator - TRL's DataCollatorForCompletionOnlyLM, or train_on_inputs false in Axolotl - and a retrain, which for LoRA SFT is a single-GPU job measured in hours.\n\nTwo things I'd rule out first. Template mismatch: if serving renders a different chat template than training, or drops the generation prompt, the model is completing the user's turn instead of starting the assistant's - render the exact inference-time token sequence and diff it against a training example. And base-model residue: a non-instruct base tuned on a few hundred examples can echo from pretraining habits, which is a data problem, not a masking one.\n\nThe discriminating test is to replay a literal training example through the training-time template offline. A masking bug still echoes. A template mismatch disappears.",
      "numbers": [],
      "red_flags": [
        "Blames the base model or \"not enough data\" without ever inspecting a collated training batch.",
        "Doesn't know the labels and ignore-index mechanics at all.",
        "Proposes to hide it - a system prompt telling the model not to echo, or stripping the echo in postprocessing."
      ],
      "followups": [
        "You turned on sequence packing and quality dropped - what happened?",
        "The model never stops generating - what do you check?",
        "How would you catch this in CI before it ships?"
      ],
      "topic": "Training"
    },
    {
      "volume": "NLP",
      "volume_name": "NLP",
      "chapter": 1,
      "chapter_title": "Classical NLP Foundations",
      "level": "L5",
      "type": "Conceptual",
      "question": "Explain BM25. Why is it still used in production search systems?",
      "answer_source": "BM25 is a probabilistic retrieval function that scores the relevance of a document to a\nquery. It improves on TF-IDF in two key ways.\n\nFirst, **term frequency saturation**: BM25 uses the formula $f(k_1+1)/(f+k_1)$ for\nthe TF component, which asymptotically approaches $k_1 + 1$ as term frequency $f$\nincreases. This means additional occurrences of a query term yield diminishing returns,\nunlike raw TF which grows linearly.\n\nSecond, **document length normalization**: the denominator includes a factor\n$k_1(1 - b + b \\cdot |d|/d_{\\text{avg}})$ that penalizes longer documents. A document\ntwice the average length needs roughly twice the term frequency to achieve the same score.\nThe parameter $b$ controls the strength of this normalization.\n\nBM25 remains in production because it is: (1) **fast**—implemented via inverted\nindex lookup, handling millions of documents in milliseconds; (2) **requires no\ntraining data**—only corpus statistics; (3) **robust**—works well across domains\nwithout tuning; (4) **interpretable**—you can explain why a document ranked highly by\ninspecting which query terms matched and their BM25 contributions; (5) **complementary\nto neural methods**—modern hybrid systems use BM25 for exact lexical matching alongside\ndense retrieval for semantic matching.",
      "red_flags_source": "- Not knowing the parameters $k_1$ and $b$ or their roles.\n \n- Saying \"BM25 is just TF-IDF\" without explaining the differences.\n \n- Not knowing that BM25 is still a production standard (Elasticsearch, Solr, Lucene).",
      "followups_source": "What are typical values for $k_1$ and $b$? How does BM25 handle multi-word\nqueries? What is BM25F? How would you combine BM25 with a dense retriever?",
      "status": "recut",
      "id": 72,
      "say": "BM25 is a bag-of-words scoring function that fixes the two places raw TF-IDF is naive, and it's still the default lexical retriever in Lucene, Elasticsearch and Solr.\n\nFirst is term-frequency saturation. In TF-IDF a document mentioning your query term twenty times scores twenty times one that mentions it once, which is obviously wrong. BM25 runs the count through a saturating function, controlled by k1, so the fifth occurrence adds far less than the second. Second is length normalization: the denominator scales with document length relative to the corpus average, controlled by b, so a document twice the average needs roughly twice the term frequency to score the same. That stops long documents winning by containing everything.\n\nIt survives because it's an inverted-index lookup, so milliseconds over hundreds of millions of documents; it needs no training data, just corpus statistics, so it works on day one in a new domain; and it's interpretable, so you can say exactly which query terms matched and what each contributed.\n\nBut the real reason is complementarity. Dense retrieval fails on exact matches - part numbers, rare proper nouns, code identifiers - which is precisely what lexical matching is good at, so every serious production system is hybrid.",
      "numbers": [
        "Typical parameters: k1 around 1.2-2.0, b around 0.75"
      ],
      "red_flags": [
        "\"BM25 is just TF-IDF\" without being able to name what changed.",
        "Doesn't know what k1 and b control.",
        "Treats it as legacy - it is the lexical half of essentially every production hybrid system."
      ],
      "followups": [
        "What are typical values for k1 and b?",
        "What is BM25F for?",
        "How would you fuse BM25 with a dense retriever?"
      ],
      "topic": "Retrieval & Ranking",
      "notes": "The source's followup asks for typical k1 and b values but never gives them; I put the standard defaults (k1 1.2-2.0, b 0.75) in `numbers` so the deck answers its own followup. Not stated in `say`."
    },
    {
      "volume": "NLP",
      "volume_name": "NLP",
      "chapter": 3,
      "chapter_title": "Word Representations and Embeddings",
      "level": "L5",
      "type": "Conceptual",
      "question": "What are the differences between Word2Vec, GloVe and FastText? When would you choose each one?",
      "answer_source": "All three produce static word embeddings but differ in their training approach\nand capabilities.\n\n**Word2Vec** (Mikolov et al., 2013) trains a shallow neural network on\nlocal context windows. Skip-gram predicts context from center word; CBOW\npredicts center from context. Training is online (streaming) and scales to\nvery large corpora. Limitation: treats words as atomic units, no OOV\nhandling.\n\n**GloVe** (Pennington et al., 2014) first computes a global\nco-occurrence matrix, then optimizes a weighted least-squares objective so\nthat word vector dot products approximate the log co-occurrence count. It\nexplicitly captures global statistics that Word2Vec's local windows might\nmiss, but requires pre-computing the co-occurrence matrix ($O(V^2)$ space,\nthough sparse in practice).\n\n**FastText** (Bojanowski et al., 2017) extends Word2Vec by representing\neach word as a bag of character n-grams. A word's vector is the sum of its\nn-gram vectors. This enables OOV handling (decompose unseen words into known\nn-grams), captures morphological structure, and produces better embeddings\nfor morphologically rich languages.\n\n**When to choose each:**\n\n \n- **Word2Vec:** General-purpose embeddings, very large corpora,\n when morphology is not a concern (e.g., English for well-curated text).\n \n- **GloVe:** When you want pre-trained vectors (Stanford provides\n high-quality pre-trained GloVe), or when you specifically need embeddings\n that capture global co-occurrence patterns.\n \n- **FastText:** Morphologically rich languages, noisy text\n (social media, user-generated content with typos), specialized domains\n with many OOV terms. Also when you need fast supervised classification\n (FastText's supervised mode).\n\nIn practice, for most modern systems, the choice between these three matters\nless than whether to use them at all versus a contextual model. But for\nlow-resource, low-latency, or edge-deployment scenarios, FastText remains a\nstrong choice.",
      "red_flags_source": "- Saying \"they are all the same, just use BERT.\"\n \n- Not knowing that GloVe uses a global co-occurrence matrix while\n Word2Vec uses local windows.\n \n- Not knowing that FastText handles OOV via character n-grams.\n \n- Being unable to articulate when static embeddings are preferred\n over contextual ones.",
      "followups_source": "How does Levy and Goldberg's result connect Word2Vec to SVD on\na PMI matrix? When would you use static embeddings in a modern system?\nHow does FastText's approach differ from BPE tokenization?",
      "status": "recut",
      "id": 77,
      "say": "All three give you one static vector per word. They differ in what they train on and whether they can handle a word they've never seen.\n\nWord2Vec trains a shallow network on local context windows - skip-gram predicts context from the center word, CBOW does the reverse. It streams, so it scales to enormous corpora, but words are atomic: an unseen word has no vector at all.\n\nGloVe goes the other way. It builds the global co-occurrence matrix first, then fits vectors so dot products approximate log co-occurrence counts, using corpus-wide statistics directly rather than hoping local windows aggregate to them. The cost is materializing that matrix, and it's equally helpless on out-of-vocabulary words.\n\nFastText is Word2Vec plus character n-grams: a word's vector is the sum of its subword n-gram vectors. That gives you OOV handling for free and captures morphology, which matters a great deal in inflected languages.\n\nSo FastText for typos, user-generated text, morphologically rich languages, or heavy domain vocabulary; GloVe if I just want good pretrained English vectors off the shelf. But the bigger decision is whether static embeddings are right at all - a contextual model beats all three when you can afford it, and static vectors earn their place on latency, memory and edge deployment.",
      "numbers": [],
      "red_flags": [
        "\"They're all the same, just use BERT\" - dodges the question and misses where static vectors still win.",
        "Doesn't know GloVe uses global co-occurrence counts while Word2Vec uses local windows.",
        "Doesn't know FastText gets OOV handling from character n-grams."
      ],
      "followups": [
        "How does Levy and Goldberg's result connect Word2Vec to SVD on a PMI matrix?",
        "When would you use static embeddings in a system today?",
        "How is FastText's n-gram decomposition different from BPE?"
      ],
      "topic": "NLP & LLMs"
    },
    {
      "volume": "NLP",
      "volume_name": "NLP",
      "chapter": 4,
      "chapter_title": "Neural Sequence Models",
      "level": "L5",
      "type": "Conceptual",
      "question": "Why did transformers replace LSTMs? What specific limitations did they address?",
      "answer_source": "Transformers replaced LSTMs by addressing three fundamental limitations:\n\n**(1) Sequential computation.** An LSTM processes tokens one at a\ntime—$\\vh_t$ depends on $\\vh_{t-1}$. This creates an $O(T)$ sequential\nbottleneck that cannot be parallelized across time steps. Transformers\nuse self-attention, where every token attends to every other token\n**simultaneously**, enabling full parallelism on GPUs/TPUs. This\ndifference makes transformers dramatically faster to train.\n\n**(2) Long-range dependency path length.** In an LSTM, information\nfrom position 1 must traverse all intermediate hidden states to reach\nposition $T$—a path of length $O(T)$. Even with the cell state, some\ndegradation occurs. In a transformer, any two positions are connected\nby a **direct** attention path of length $O(1)$, making long-range\ndependencies much easier to capture.\n\n**(3) Scalability.** LSTMs do not benefit proportionally from\nlarger models and datasets. Transformers exhibit favorable\n**scaling laws**: performance improves predictably as a power law\nwith compute, data, and parameters. This enabled the scale-up from\nBERT (340M) to GPT-3 (175B) to modern frontier models.",
      "red_flags_source": "- Saying \"transformers are just better\" without explaining why.\n\n- Forgetting the parallelization advantage (the most important\n practical reason).\n\n- Not mentioning path length for long-range dependencies.\n\n- Claiming LSTMs are completely obsolete (they still have niche uses).",
      "followups_source": "What is the computational complexity of self-attention?\nWhere might you still use an LSTM today? What are the limitations of\ntransformers that LSTMs do not have?",
      "status": "recut",
      "id": 82,
      "say": "Three reasons, and the first is the one that actually decided it: parallelism. An LSTM's hidden state at step t depends on step t minus one, so training is a sequential chain as long as the sequence and you simply can't use the hardware. Self-attention computes all positions at once, so a training step is a few large matrix multiplies that saturate a GPU. That's orders of magnitude in wall-clock, and it's what made scaling possible at all.\n\nSecond, path length. In an LSTM, information from position one reaches position T through every intermediate state, degrading along the way even with the gating. In a transformer any two positions are one attention hop apart, so a long-range dependency doesn't have to survive the journey.\n\nThird, and downstream of the first two, scaling behavior. Transformers keep improving predictably as you add parameters, data and compute - that's what the scaling laws describe - while LSTMs plateau. Once that was visible everything followed: BERT at a few hundred million, GPT-3 at 175 billion.\n\nWhat you give up is the constant-size state. Attention is quadratic in sequence length where an LSTM step is O(1) in memory, which is genuinely better for streaming and very long inputs, and it's why constant-state architectures keep resurfacing.",
      "numbers": [
        "Attention is O(n^2) in sequence length; an LSTM step is O(1) state"
      ],
      "red_flags": [
        "\"Transformers are just better\" with no mechanism.",
        "Leads with attention quality and never mentions parallel training, which is the practical reason.",
        "Claims LSTMs are entirely obsolete; constant-state models still win on streaming and very long inputs."
      ],
      "followups": [
        "What's the computational complexity of self-attention, and what does it cost you?",
        "Where would you still use an LSTM today?",
        "What do transformers do worse than LSTMs?"
      ],
      "topic": "Architectures"
    },
    {
      "volume": "NLP",
      "volume_name": "NLP",
      "chapter": 5,
      "chapter_title": "Transformers and Modern Architectures",
      "level": "L5",
      "type": "Conceptual",
      "question": "Compare encoder-only, decoder-only and encoder-decoder transformers. Why did decoder-only dominate the LLM era?",
      "answer_source": "**Encoder-only** (BERT): bidirectional attention, trained with MLM, excels\nat understanding/classification tasks but cannot generate text natively.\n**Decoder-only** (GPT): causal attention, trained autoregressively,\nnatural for generation and showed emergent in-context learning at scale.\n**Encoder-decoder** (T5): bidirectional encoder feeds into causal decoder\nvia cross-attention, strong for structured input-output tasks like translation.\n\nDecoder-only dominated because: (1)~architectural simplicity—one stack, not\ntwo; (2)~CLM scales naturally with internet-scale data; (3)~in-context learning\nemerged, eliminating the need for task-specific fine-tuning; (4)~KV-cache\nworks cleanly for causal models—previously computed KVs never change.\nEncoder-decoder still wins for some structured tasks but decoder-only's\nflexibility and scaling properties proved decisive.",
      "red_flags_source": "- Cannot articulate the attention masking difference\n \n- Says \"GPT is better than BERT\" without nuance\n \n- Does not mention in-context learning as a key factor\n \n- Confuses training objectives (MLM vs.\\ CLM)",
      "followups_source": "When would you still choose BERT over an LLM? What is prefix\nlanguage modeling? Could encoder-decoder make a comeback?",
      "status": "recut",
      "id": 87,
      "say": "Three shapes, and the difference is entirely in the attention mask and the training objective. Encoder-only, meaning BERT, is bidirectional: every token sees every other token, trained with masked language modeling. Great for classification and understanding, but it can't generate natively. Decoder-only, meaning GPT, is causal: each token sees only what came before it, trained to predict the next token. Encoder-decoder, meaning T5, has a bidirectional encoder feeding a causal decoder through cross-attention, which suits structured input-to-output tasks like translation.\n\nDecoder-only won for four reasons. It's one stack instead of two, so there's less to build and less to tune. Next-token prediction scales cleanly on raw internet text - no masking scheme to design, no paired data to collect. In-context learning emerged from it at scale, which meant you stopped needing a separately fine-tuned model per task. And the KV cache works cleanly under causal masking, because previously computed keys and values never change, which is what makes serving tractable.\n\nEncoder-decoder still wins on some structured tasks where you genuinely have a fixed input to condition on, and encoder-only models are still the right call for high-throughput classification and embeddings, where you don't need generation and you do want bidirectional context.",
      "numbers": [],
      "red_flags": [
        "Can't state the attention masking difference; that is the whole distinction.",
        "\"GPT is better than BERT\" with no task framing.",
        "Never mentions in-context learning as the reason per-task fine-tuning stopped mattering."
      ],
      "followups": [
        "When would you still choose BERT over an LLM?",
        "What is prefix language modeling?",
        "Could encoder-decoder make a comeback?"
      ],
      "topic": "Architectures"
    },
    {
      "volume": "NLP",
      "volume_name": "NLP",
      "chapter": 7,
      "chapter_title": "Large Language Models and In-Context Learning",
      "level": "L5",
      "type": "Conceptual",
      "question": "Explain scaling laws for LLMs. What does the Chinchilla paper tell us about compute-optimal training?",
      "answer_source": "Scaling laws describe the empirical finding that language model loss follows\na power law with respect to model size $N$, dataset size $D$, and total\ncompute $C$: $L(N) \\propto N^{-\\alpha_N}$, $L(D) \\propto D^{-\\alpha_D}$,\n$L(C) \\propto C^{-\\alpha_C}$. The total compute is approximately\n$C \\approx 6ND$ (forward + backward pass FLOPs).\n\nThe Chinchilla paper (Hoffmann et al., 2022) showed that for a fixed compute\nbudget, the optimal allocation is to scale model size and data proportionally,\nwith tokens $\\approx 20\\times$ parameters. This revealed that GPT-3 (175B\nparams, 300B tokens) was severely undertrained—Chinchilla (70B params, 1.4T\ntokens) matched it with far fewer parameters. In practice, most production\nmodels are **over-trained** beyond the Chinchilla ratio (e.g., LLaMA-3 8B\ntrained on 15T tokens, which is $\\sim$94$\\times$ the Chinchilla-optimal) to\nreduce inference cost—you pay the extra training cost once, but enjoy a\nsmaller, cheaper model for all future queries.",
      "red_flags_source": "- Stating the Chinchilla ratio as a rigid rule without understanding\n the over-training rationale.\n\n- Not knowing the $C \\approx 6ND$ relationship.\n\n- Confusing compute-optimal (minimizes loss for fixed FLOPs) with\n inference-optimal (minimizes serving cost).\n\n- Hand-waving about \"bigger models are better\" without quantitative\n reasoning.",
      "followups_source": "How do you estimate the total FLOPs for training a model? What\nhappens when you run out of high-quality data? How do scaling laws change\nfor fine-tuning vs.\\ pretraining?",
      "status": "recut",
      "id": 92,
      "say": "Scaling laws are the empirical finding that language model loss falls as a power law in model size, dataset size and compute - no plateau in the ranges anyone has measured, and predictable enough that you fit the curve at small scale and extrapolate a run you haven't done yet. The bridge from a budget to a model is that training compute is about six N D: roughly six FLOPs per parameter per token, forward and backward.\n\nChinchilla showed that for a fixed compute budget people were allocating it wrong. The optimum scales parameters and tokens together, roughly twenty tokens per parameter. GPT-3, at 175 billion parameters on 300 billion tokens, was badly undertrained; Chinchilla at 70 billion on 1.4 trillion matched it with a fraction of the parameters.\n\nThen the part where people get stuck: almost nobody trains Chinchilla-optimal anymore, and that's deliberate. Chinchilla minimizes loss for a fixed training budget and ignores inference entirely. If you're going to serve a model billions of times you want the smallest model that clears your quality bar, so you overtrain hard - Llama 3 8B on 15 trillion tokens is roughly ninety times the Chinchilla-optimal ratio. You pay the training cost once and collect cheaper serving forever.",
      "numbers": [
        "Training compute C ~ 6ND FLOPs",
        "Chinchilla: ~20 tokens per parameter",
        "Llama 3 8B: 15T tokens, ~94x the Chinchilla-optimal token ratio"
      ],
      "red_flags": [
        "Quotes 20:1 as a rule to obey rather than a compute-optimal point that ignores inference cost.",
        "Doesn't know C ~ 6ND, so can't turn a compute budget into a model size.",
        "Confuses compute-optimal with inference-optimal."
      ],
      "followups": [
        "Estimate the FLOPs to train a 70B model on 2T tokens.",
        "What happens when you run out of high-quality data?",
        "Do scaling laws look the same for fine-tuning?"
      ],
      "topic": "NLP & LLMs"
    },
    {
      "volume": "NLP",
      "volume_name": "NLP",
      "chapter": 8,
      "chapter_title": "RLHF and Alignment",
      "level": "L5",
      "type": "Conceptual",
      "question": "Walk me through the RLHF pipeline step by step. What are the three stages?",
      "answer_source": "The RLHF pipeline has three stages.\n\n**Stage 1—SFT:** Fine-tune the pretrained model on high-quality\ndemonstration data (instruction-response pairs) to teach it the format and\nstyle of helpful responses. This produces the SFT model, which also serves\nas the reference policy $\\pi_{\\text{ref}}$ for subsequent stages.\n\n**Stage 2—Reward Model Training:** Collect human preferences: for\neach prompt, generate two (or more) responses, and have annotators indicate\nwhich is better. Train a reward model using the Bradley-Terry framework:\n$\\loss_{\\text{RM}} = -\\E[\\log \\sigma(r(x, y_w) - r(x, y_l))]$.\nThe RM shares the base model architecture but replaces the LM head with a\nscalar output.\n\n**Stage 3—RL Optimization (PPO):** Optimize the policy to maximize\n$\\E[r(x, y)] - \\beta \\cdot \\KL(\\pi_\\theta \\| \\pi_{\\text{ref}})$. PPO\nuses a clipped surrogate objective for stable updates. The KL penalty\nprevents reward hacking by keeping the policy close to the SFT model.\nThis stage requires four models in memory: policy, reference, reward model,\nand value function (critic).",
      "red_flags_source": "- Cannot name all three stages or gets the order wrong.\n\n- Skips the reward model and jumps from SFT to RL.\n\n- Does not mention the KL penalty or its purpose.\n\n- Confuses the reward model with the value function.",
      "followups_source": "How many models do you need in GPU memory during PPO? What\nhappens if the reward model is poor? Why is the SFT model used as the\nreference rather than the pretrained model?",
      "status": "recut",
      "id": 97,
      "say": "Three stages: supervised fine-tuning, then a reward model, then RL against that reward.\n\nSFT first. You fine-tune the pretrained base on high-quality demonstration data, instruction and response pairs, so the model learns the format and register of a helpful answer. That checkpoint does double duty - it's the starting policy, and it becomes the reference the KL penalty is measured against later.\n\nStage two, the reward model. For each prompt you sample two or more responses and have annotators say which is better, then train under Bradley-Terry - a logistic loss on the difference in scores between preferred and dispreferred - so the model only ever learns relative quality, never an absolute scale. Architecturally it's the same base model with the language modeling head swapped for a scalar head.\n\nStage three, PPO. Optimize the policy to maximize expected reward minus beta times the KL from the reference. The KL term is not optional: without it the policy walks off into whatever degenerate text the reward model happens to score highly, which is reward hacking.\n\nThe practical detail interviewers like: stage three holds four models in memory at once - policy, frozen reference, reward model, and the value function critic. That footprint is a big part of why DPO got popular.",
      "numbers": [
        "PPO stage holds 4 models in memory: policy, reference, reward model, critic"
      ],
      "red_flags": [
        "Gets the order wrong, or jumps from SFT straight to RL with no reward model.",
        "Doesn't mention the KL penalty, or can't say what it prevents.",
        "Confuses the reward model with the value function; different models, different jobs."
      ],
      "followups": [
        "How many models are resident during PPO?",
        "What happens if the reward model is weak?",
        "Why is the SFT model the reference rather than the pretrained base?"
      ],
      "topic": "Alignment & Safety"
    },
    {
      "volume": "NLP",
      "volume_name": "NLP",
      "chapter": 9,
      "chapter_title": "Retrieval-Augmented Generation",
      "level": "L5",
      "type": "Trade-off",
      "question": "When should you use RAG vs. fine-tuning vs. long context?",
      "answer_source": "These serve different purposes and are not mutually exclusive.\n\n**RAG:** when knowledge changes frequently, when you need attribution and\ncitations, when the knowledge base is too large for any context window, and when\nverifiability matters (legal, medical, compliance). RAG's knowledge can be\nupdated by re-indexing without retraining.\n\n**Fine-tuning:** when you need to change the model's **behavior**—\nits tone, format, instruction-following style, or domain reasoning patterns.\nFine-tuning encodes knowledge into weights, so it cannot be easily updated\nand provides no attribution. Best for: style adaptation, domain-specific\ninstruction following, distillation.\n\n**Long context:** when the corpus is small enough to fit in a context\nwindow, when you need real-time document analysis (\"summarize this PDF\"),\nor when pipeline simplicity is paramount. Limitations: cost scales linearly\nwith context length, \"lost in the middle\" problem, context window is still\nfinite.\n\n**Combine:** fine-tune + RAG is the most common production pattern\n(fine-tune for domain behavior, RAG for knowledge grounding). Long context\n+ RAG is emerging: retrieve a larger set and let the model process everything.",
      "red_flags_source": "- Treats these as mutually exclusive\n \n- Says \"just use RAG for everything\" or \"just fine-tune\"\n \n- Cannot articulate when fine-tuning beats RAG (behavior vs.\\ knowledge)\n \n- Ignores cost, latency, and updateability considerations",
      "followups_source": "What is RAFT (Retrieval Augmented Fine-Tuning)? How do the\ntradeoffs change as context windows grow to 1M+ tokens? When would you\nfine-tune the retrieval model vs.\\ the generation model?",
      "status": "recut",
      "id": 102,
      "say": "They solve different problems, so the question is which one you need, and usually the answer is more than one. The clean split: RAG is for knowledge, fine-tuning is for behavior, long context is for a document you have in hand right now.\n\nRAG when the knowledge changes, when you need citations, when the corpus is far too big for any context window, or when someone has to audit why the model said what it said - legal, medical, compliance. Its real advantage is that you update it by re-indexing, with no training run at all.\n\nFine-tuning when you want to change how the model behaves rather than what it knows: tone, output format, instruction-following style, domain reasoning patterns, or distilling a big model into a small one. Knowledge baked into weights can't be updated cheaply and carries no attribution, which is why using fine-tuning as a knowledge store disappoints.\n\nLong context when the corpus fits, when the task genuinely is \"analyze this PDF,\" or when pipeline simplicity beats efficiency. The limits are that cost scales with context length on every call, retrieval inside a long context degrades in the middle, and the window is still finite.\n\nIn production it's usually fine-tune for behavior plus RAG for knowledge.",
      "numbers": [],
      "red_flags": [
        "Treats them as three competing options rather than complementary layers.",
        "Can't articulate the behavior-versus-knowledge line, so recommends fine-tuning to teach facts.",
        "Ignores update cadence, per-call cost, and attribution requirements."
      ],
      "followups": [
        "What is RAFT, retrieval-augmented fine-tuning?",
        "How do the trade-offs change at million-token context windows?",
        "Would you fine-tune the retriever or the generator?"
      ],
      "topic": "Retrieval & Ranking"
    },
    {
      "volume": "SR",
      "volume_name": "Search & Rec",
      "chapter": 1,
      "chapter_title": "Search Systems and Query Understanding",
      "level": "L6",
      "type": "Debugging",
      "question": "Your new L2 reranker improves offline NDCG by 4%, but online conversions are flat. Walk me through your diagnosis.",
      "answer_source": "Diagnose the funnel stage by stage, starting above the reranker.\n\n**(1) The recall ceiling first.** The reranker reorders whatever retrieval hands it; if the candidate sets lack the genuinely good documents, a better ordering of mediocre candidates moves nothing. Judged-only offline eval hides this—the judged pool was built from what past systems retrieved. Check recall per stage against judged-relevant documents; if retrieval recall is the binding constraint, the fix is upstream of the model you just shipped.\n\n**(2) Offline label contamination.** If the offline labels derive from clicks, they inherit the old ranker's position bias—\"improvement\" partly measures agreement with the incumbent plus noise (Chapter~). Re-evaluate on the bias-resistant anchors: editorial judgments and any randomized-traffic slice.\n\n**(3) Where are the gains?** Slice the offline delta by segment and by position: gains concentrated on torso/tail queries that are a small share of traffic, or at positions below the fold, are real but commercially invisible. Traffic-weight the offline gain before expecting an online echo.\n\n**(4) What actually got served?** L3 blending—business rules, merchandising, diversity—may be clobbering the new order. Measure the override rate: how often does the served order differ from the ranker's order, and did it change?\n\n**(5) Presentation and metric distance.** A reordering with identical titles/images/prices may not change click behavior; and conversion is far downstream—check the intermediate chain (CTR@k, add-to-cart, reformulation rate) for movement. Finally, check power: the experiment may simply be underpowered for a conversion-sized effect (Chapter~).",
      "red_flags_source": "- \"The offline metric must be wrong\" or \"ship it anyway\"—no decomposition, no diagnosis.\n \n- Not reaching for per-stage recall—the recall-ceiling check is the expected first move.\n \n- Never mentioning click-label position bias when the offline labels are behavioral.",
      "followups_source": "\"How do you maintain a randomized slice cheaply and ethically?\" \"The gains were all on tail queries—what would make them show up in conversions?\" \"Offline says worse, online says better—now what?\"",
      "status": "recut",
      "id": 107,
      "say": "Flat conversions on a four-point offline gain usually means the gain was never reachable, never served, or never measurable. I'd work the funnel from above the reranker down - the model is the last suspect.\n\nStart with the recall ceiling. The reranker only reorders what retrieval hands it, so if the candidate set lacks the genuinely good documents, a better ordering of mediocre ones changes nothing. Judged-only offline eval hides this, because the judgment pool was built from what past systems retrieved. Measure per-stage recall against judged-relevant documents; if retrieval is binding, the fix is upstream of what you shipped.\n\nThen question the offline number. If those labels came from clicks they carry the old ranker's position bias, so part of your four percent is measuring agreement with the incumbent. Re-run against editorial judgments or a randomized-traffic slice, which don't have that circularity.\n\nThen ask where the gain lives. Slice the offline delta by segment and by position: gains on tail queries that are a sliver of traffic, or at positions below the fold, are real and commercially invisible. Traffic-weight the offline delta before expecting an online echo.\n\nFinally, what actually got served - L3 business rules, merchandising and diversity constraints may be overriding the new order, so measure the override rate - and check power, since conversion is downstream and low-rate. Look at CTR and add-to-cart for movement, and confirm the test could detect a conversion-sized effect.",
      "numbers": [],
      "red_flags": [
        "\"The offline metric must be wrong\" or \"ship it anyway\" - no decomposition, no diagnosis.",
        "Never reaches for per-stage recall; the recall-ceiling check is the expected first move.",
        "Never questions click-derived labels when the offline metric was built on behavior."
      ],
      "followups": [
        "How do you maintain a randomized slice cheaply and ethically?",
        "The gains were all on tail queries - what would make them show up in conversions?",
        "Offline says worse, online says better. Now what?"
      ],
      "topic": "Retrieval & Ranking",
      "notes": "The `question` field was truncated at the LaTeX \\% escape (\"...NDCG by 4\\\"). Restored from volumes/search-recommendation/sections/01_search_systems_query_understanding.tex. This question overlaps heavily with id 112; see the note there."
    },
    {
      "volume": "SR",
      "volume_name": "Search & Rec",
      "chapter": 4,
      "chapter_title": "Neural Retrieval and Reranking",
      "level": "L6",
      "type": "Debugging",
      "question": "Your new reranker improved offline NDCG by 4%, but online CTR dropped. Walk me through the diagnosis.",
      "answer_source": "**Frame it as a funnel + measurement problem**—the model is the last suspect, not the first. Offline NDCG and online CTR disagree for a small set of well-known reasons; walk them in cost order.\n\n**Hypothesis 1: latency regression.** The new reranker is slower; pages arrive later; engagement drops for reasons having nothing to do with ordering—latency is itself a relevance metric ($\\sim$100\\,ms delays measurably reduce engagement). **Check first, it is the cheapest:** p95 end-to-end latency and timeout/degradation rates in the treatment arm. A heavier model that blows its L2 budget can also silently shrink rerank depth (100 $\\to$ 20 candidates), capping quality through the recall ceiling while offline eval—run without the budget—shows a gain.\n\n**Hypothesis 2: the offline eval was rigged in your favor.** (a) **Judged-only / condensed-list bias:** offline NDCG computed over judged candidates only, while online the reranker meets the full candidate distribution, including strata the judgment pool never covered. (b) **Click-derived labels are circular:** labels inherited the old ranker's position bias, so \"NDCG up\" partly measures agreement-with-the-old-system + noise; a genuinely different ordering can score worse with users while scoring better on biased labels (unbiased-LTR discipline, Chapter~). (c) **Segment mismatch:** gains concentrated on torso/tail slices that are a small traffic share, losses on head queries that dominate impressions. Re-slice both offline and online metrics by segment before concluding anything.\n\n**Hypothesis 3: the page, not the ranking.** CTR responds to **presentation**: the new order surfaces documents with worse titles/snippets/images; or an answer/snippet module now satisfies the user without a click (\"good abandonment\")—CTR down can mean success. Check downstream success metrics (conversion, satisfied-session rate, reformulation), not clicks alone.\n\n**Hypothesis 4: L3 interference and score-consumer breakage.** Business rules reorder the new top-20 differently (the L2 gain never reaches the page—check L3 override rates); or downstream consumers of the reranker **score**—thresholds, blending, vertical merge—were calibrated to the old model's score distribution and are now misfiring (Section~).\n\n**Hypothesis 5: recall ceiling misattribution.** The reranker reorders a candidate set that lacks good documents for the affected slices; offline eval used a richer candidate pool than production L0 provides. Check per-stage recall on live traffic samples.\n\n**Order of operations:** latency/timeout dashboards $\\to$ segment-sliced online deltas $\\to$ L3 override and score-consumer audit $\\to$ candidate-set recall on live traffic $\\to$ label-bias audit of the offline eval. Most real incidents resolve at steps one, two, or four.",
      "red_flags_source": "- Concluding \"offline metrics are useless\" or \"retrain on fresh clicks\" without a diagnosis.\n \n- Never mentioning latency—the most common real cause and the cheapest to check.\n \n- Treating CTR as ground truth (no good-abandonment or presentation reasoning), or NDCG-on-click-labels as ground truth (no circularity reasoning).",
      "followups_source": "\"Which single dashboard do you check first and why?\" \"How would you build an offline eval that would have predicted this?\" \"CTR dropped but conversions rose—what does that tell you?\"",
      "status": "recut",
      "id": 112,
      "say": "CTR dropping is different from CTR being flat - something actively got worse - and the cheapest check is also the most common cause: latency. A heavier reranker makes pages arrive later, and latency is itself a relevance metric; a hundred milliseconds measurably moves engagement. Worse, if the model blows its L2 time budget the system may silently shrink rerank depth from a hundred candidates to twenty, capping you at the recall ceiling online while the offline eval ran with no budget at all. So: p95 latency and timeout rates in the treatment arm, first.\n\nSecond, presentation and what CTR actually measures. Clicks respond to titles, snippets and images, not relevance in the abstract, so a better ordering that surfaces uglier results loses clicks. And clicks can drop because you succeeded - a good snippet satisfies the user without one. Check conversion, satisfied-session rate and reformulation before calling it a regression.\n\nThird, L3 and score consumers. Business rules may be reordering your new top twenty, so check override rates. And anything calibrated on the old score distribution - thresholds, vertical blending, merge rules - is now misfiring on scores that mean something different.\n\nFourth, the offline eval itself: judged-only NDCG sees a candidate pool production doesn't, click labels inherit the old ranker's position bias, and gains on tail segments can hide losses on head queries that dominate impressions.\n\nMy order: latency dashboards, segment-sliced online deltas, L3 and score-consumer audit, then the label-bias audit.",
      "numbers": [
        "~100 ms of added latency measurably reduces engagement"
      ],
      "red_flags": [
        "Concludes \"offline metrics are useless\" or retrains on fresh clicks with no diagnosis.",
        "Never mentions latency - the most common real cause and the cheapest to check.",
        "Treats CTR as ground truth, with no good-abandonment or presentation reasoning."
      ],
      "followups": [
        "Which single dashboard do you open first, and why?",
        "How would you build an offline eval that would have predicted this?",
        "CTR dropped but conversions rose - what does that tell you?"
      ],
      "topic": "Retrieval & Ranking",
      "notes": "The `question` field was truncated at the LaTeX \\% escape; restored from volumes/search-recommendation/sections/04_neural_retrieval_reranking.tex. DROP CANDIDATE: this is a near-duplicate of id 107 (same scenario, CTR instead of conversions, same funnel diagnosis). I kept both but split the emphasis - 107 leads on recall ceiling and offline label bias, 112 leads on latency and presentation. If the deck needs 29 questions instead of 30, cut 107 and keep 112, which has the latency-first move."
    },
    {
      "volume": "SR",
      "volume_name": "Search & Rec",
      "chapter": 6,
      "chapter_title": "Recommendation Systems",
      "level": "L5",
      "type": "Conceptual",
      "question": "How do you handle the cold start problem for new users and new items?",
      "answer_source": "Cold start requires different strategies for users vs. items, and the key insight is that it is a **data** problem, not a **model** problem—the best solution is to design the system to collect useful signals quickly.\n\n**New users (no interaction history):**\n\n \n- Start with popularity-based and content-based recommendations (demographics, device, location, referral source).\n \n- Ask explicit preferences during onboarding (\"select genres you enjoy\").\n \n- Use contextual bandits (Thompson sampling, epsilon-greedy) to quickly learn preferences from the first few interactions.\n \n- Leverage transfer signals: if the user signed in with a social account, use social graph features.\n\n**New items (no user interactions):**\n\n \n- Use item content features (text description, images, category, price) to place the item in embedding space near similar known items.\n \n- Boost exploration: deliberately show the new item to a small percentage of users to collect interaction data. Use inverse propensity weighting to correct for the exploration bias.\n \n- Use CLIP-style cross-modal embeddings to bridge the gap between content features and collaborative signals.",
      "red_flags_source": "- \"Just use collaborative filtering\" (impossible without interaction data)\n \n- Ignoring the exploration-exploitation tradeoff\n \n- Not distinguishing between user cold start and item cold start",
      "followups_source": "\"How do you balance exploration vs. exploitation?\" \"When does cold start stop being a problem?\" \"How would you evaluate whether your cold start strategy is working?\"",
      "status": "recut",
      "id": 117,
      "say": "Cold start is a data problem, not a modeling problem: the system's job is to collect useful signal fast, and the model is downstream of that.\n\nSplit it into users and items, because they need opposite things. For a new user you have no interactions but you're not blind - device, locale, referral source, sometimes a social login. Serve popularity and content-based recommendations conditioned on whatever you have, ask for a few explicit preferences at onboarding if the product tolerates it, and run a contextual bandit, Thompson sampling or epsilon-greedy, so the first few interactions actively buy information instead of exploiting a weak prior.\n\nFor a new item it's the opposite: rich content, no behavior. Use content features - title, description, images, category, price - to place the item near similar known items in embedding space so it inherits their collaborative signal until it has its own, which is where CLIP-style cross-modal embeddings help. Then explicitly budget exploration: force it in front of a small slice of traffic to earn interaction data, and apply inverse propensity weighting when you train on that data so you don't bake the exploration policy into the model.\n\nThe failure I'd call out is treating exploration purely as a cost. If nothing is shown, nothing is learned, and the tail of the catalog stays permanently cold.",
      "numbers": [],
      "red_flags": [
        "\"Just use collaborative filtering\" - there is nothing to collaborate on yet.",
        "No exploration story, so new items never accumulate the data that would rank them.",
        "Doesn't separate user cold start from item cold start; they need opposite fixes."
      ],
      "followups": [
        "How do you balance exploration against exploitation here?",
        "When does an item stop being cold?",
        "How would you evaluate whether the cold-start path is working?"
      ],
      "topic": "Recommenders"
    },
    {
      "volume": "SR",
      "volume_name": "Search & Rec",
      "chapter": 6,
      "chapter_title": "Recommendation Systems",
      "level": "L5",
      "type": "Conceptual",
      "question": "Why do recommendation models need calibration? What happens if pCTR is systematically over-confident?",
      "answer_source": "**Why calibration matters.** In a recommendation or ads system, the model's predicted probability is not just used for **ranking**—it is used for **decision-making**. Specifically:\n\n \n- **Auction pricing**: In ads, the ad rank score is $\\text{pCTR} \\times \\text{bid}$, and the price charged to the advertiser is a function of pCTR. If pCTR is wrong, the pricing is wrong.\n \n- **Budget pacing**: Advertisers set daily budgets. The system estimates how much each impression will cost (based on pCTR) to pace spending evenly. Miscalibrated pCTR causes incorrect pacing.\n \n- **Threshold-based decisions**: Some systems use absolute probability thresholds (e.g., suppress ads with pCTR $< 0.001$). Miscalibration shifts which ads pass these thresholds.\n\n**What happens if pCTR is systematically over-confident (e.g., 2$\\times$ actual):**\n\n \n- **Advertisers overpay.** The auction charges based on pCTR, so advertisers pay for clicks they are unlikely to receive. Ad ROI drops.\n \n- **Budget exhaustion.** Advertisers with daily budget caps burn through them faster (the system thinks each impression is more valuable than it is). Ads stop showing by mid-day, leaving inventory unfilled in the afternoon.\n \n- **Reduced fill rate.** With advertiser budgets exhausted early, fewer ads compete for impressions later in the day, reducing revenue.\n \n- **Advertiser churn.** Advertisers who consistently overpay relative to actual performance reduce their bids or leave the platform.\n \n- **Ranking preserved.** Note that if the over-confidence is uniform across all ads (e.g., all pCTRs are 2$\\times$ too high), the **ranking** is unchanged—the same ads win. The problem is entirely in the pricing and budgeting. This is why AUC (a ranking metric) can be high while revenue drops.",
      "red_flags_source": "- \"Calibration does not matter if the ranking is correct\" (in ads, pricing depends on absolute probabilities)\n \n- Not distinguishing between ranking quality and calibration quality\n \n- Thinking calibration is only a \"nice to have\"",
      "followups_source": "\"How would you measure calibration?\" \"What calibration method would you use and why?\" \"How often do you need to recalibrate?\"",
      "status": "recut",
      "id": 122,
      "say": "Because the predicted probability isn't only used for ordering - it's consumed as a number in downstream decisions, and those break when it's wrong even if the ranking is perfect.\n\nThree places it's used as an absolute value. Auction pricing: ad rank is pCTR times bid, and what the advertiser is charged is a function of pCTR, so miscalibration is directly mispricing. Budget pacing: the system estimates expected spend per impression from pCTR to spread a daily budget evenly. And absolute thresholds - suppress anything below some pCTR - where a shift changes which items pass at all.\n\nIf pCTR is systematically two times too high, here's the chain. Advertisers overpay for clicks that don't arrive, so measured ROI drops. Budgets burn out faster, so campaigns go dark by mid-afternoon, leaving inventory unfilled with fewer bidders competing - revenue falls at exactly the time of day you needed it. Then the slow damage: advertisers who consistently overpay lower their bids or leave.\n\nAnd here's what makes it insidious. If the overconfidence is uniform, the ranking is completely unchanged. The same ads win in the same order, AUC looks great, every ranking metric on your dashboard looks great. All the damage is in pricing and pacing, which is exactly why calibration has to be monitored separately from AUC.",
      "numbers": [
        "Ad rank = pCTR x bid, so a 2x-overconfident pCTR leaves ranking untouched and doubles implied value"
      ],
      "red_flags": [
        "\"Calibration doesn't matter if the ranking is correct\" - true for ordering, false for pricing and pacing.",
        "Can't separate ranking quality from calibration quality, or assumes AUC covers both.",
        "Treats calibration as a nice-to-have rather than a revenue-critical property."
      ],
      "followups": [
        "How would you measure calibration?",
        "Which recalibration method would you use, and why?",
        "How often do you need to recalibrate?"
      ],
      "topic": "Recommenders"
    },
    {
      "volume": "CML",
      "volume_name": "Classical ML",
      "chapter": 1,
      "chapter_title": "Supervised Learning Core",
      "level": "L5",
      "type": "Mathematical",
      "question": "Derive the normal equations for linear regression. When would you not use the closed form?",
      "answer_source": "**Derivation.** Minimize $J(\\beta)=\\norm{y-X\\beta}^2 = y^{\\top}y - 2\\beta^{\\top}X^{\\top}y + \\beta^{\\top}X^{\\top}X\\beta$. Then $\\nabla_\\beta J = -2X^{\\top}y + 2X^{\\top}X\\beta$, and setting it to zero gives $X^{\\top}X\\hat\\beta = X^{\\top}y$, so $\\hat\\beta = (X^{\\top}X)^{-1}X^{\\top}y$ when $X$ has full column rank. The Hessian $2X^{\\top}X$ is PSD, so the stationary point is a global minimum. Say the geometry as you write it: the condition is $X^{\\top}(y-X\\hat\\beta)=0$, i.e., the residual is orthogonal to the column space—OLS is the orthogonal projection of $y$ onto $\\mathrm{col}(X)$, with $H = X(X^{\\top}X)^{-1}X^{\\top}$ the projection matrix. **When not to.** Four separate reasons, not one: (i) **cost**—$O(nd^2 + d^3)$ time and $O(d^2)$ memory, so at $d=10^4$ the Gram matrix alone is $800$\\,MB and the multiply is $10^{14}$ flops, versus $O(ndk)$ for gradient descent; (ii) **singularity**—if $d>n$ or any feature is an exact combination of others (the dummy-variable trap), $X^{\\top}X$ has no inverse; (iii) **conditioning**—$\\kappa(X^{\\top}X)=\\kappa(X)^2$, so forming the Gram matrix costs you half your significant digits under near-collinearity; (iv) **the setting**—streaming or distributed data, or a non-quadratic loss, admits no closed form at all. And the implementation point that separates candidates: **never invert**. Solve by Cholesky when the problem is well conditioned and $d\\ll n$, by QR on $X$ directly for numerical safety (it works at $\\kappa(X)$, not $\\kappa(X)^2$), by SVD when $X$ may be rank-deficient—which returns the minimum-norm solution, and is exactly why `numpy.linalg.lstsq` returns an answer where the textbook formula would divide by zero. Ridge is the other fix: $X^{\\top}X + \\lambda I$ is positive definite for any $X$.",
      "red_flags_source": "- Writing `inv(X.T @ X) @ X.T @ y` as the recommended implementation.\n \n- Naming only \"it is slow\" and missing singularity and conditioning.\n \n- Not knowing the geometric interpretation, or that the residual is orthogonal to the fitted values.\n \n- Claiming OLS needs normally distributed features or targets—normality is about the **errors**, and only for inference.",
      "followups_source": "\"What is $\\kappa(X^{\\top}X)$ versus $\\kappa(X)$, and why does it matter?\" \"What does the SVD give you when $X$ is rank-deficient?\" \"How does leverage $h_{ii}$ let you compute LOOCV in one fit?\"",
      "status": "recut",
      "id": 127,
      "say": "Minimize the squared residual norm, expand, take the gradient, set it to zero: X-transpose X beta-hat equals X-transpose y, so beta-hat is the inverse of X-transpose X times X-transpose y, whenever X has full column rank. The Hessian is two X-transpose X, positive semidefinite, so that stationary point is a global minimum.\n\nThe thing to say while writing it is the geometry: the condition is X-transpose times the residual equals zero, so the residual is orthogonal to the column space. OLS is the orthogonal projection of y onto the span of your features.\n\nWhen wouldn't I use it? Four separate reasons. Cost - it's cubic in the number of features, and at ten thousand features the Gram matrix alone is 800 megabytes. Singularity - more features than rows, or any exactly collinear feature like the dummy variable trap, and there's no inverse. Conditioning - the condition number of X-transpose X is the square of the condition number of X, so forming the Gram matrix costs you half your significant digits. And the setting - streaming or distributed data, or a non-quadratic loss, has no closed form at all.\n\nThen the implementation point that separates candidates: never call inverse. Cholesky when it's well conditioned, QR on X directly when it isn't, SVD when X may be rank deficient - which is what lstsq does, and why it returns a minimum-norm answer where the textbook formula would divide by zero.",
      "numbers": [
        "Cost O(n d^2 + d^3); at d = 10,000 the Gram matrix alone is ~800 MB",
        "kappa(X^T X) = kappa(X)^2 - forming the Gram matrix squares the condition number"
      ],
      "red_flags": [
        "Offers inv(X.T @ X) @ X.T @ y as the recommended implementation.",
        "Names only \"it's slow\" and misses singularity and conditioning.",
        "Claims OLS needs normally distributed features or targets; normality is about the errors, and only for inference."
      ],
      "followups": [
        "Why does kappa(X^T X) = kappa(X)^2 matter in practice?",
        "What does the SVD give you when X is rank-deficient?",
        "How does leverage let you compute LOOCV from a single fit?"
      ],
      "topic": "Classical ML",
      "whiteboard": "$X^{\\top}X\\hat{\\beta} = X^{\\top}y \\;\\Rightarrow\\; \\hat{\\beta} = (X^{\\top}X)^{-1}X^{\\top}y$, then directly underneath it $X^{\\top}(y - X\\hat{\\beta}) = 0$. Write the second line and say it out loud: the residual is orthogonal to every column of X, so OLS is a projection. That one line is what turns a memorized formula into a derivation, and it sets up the whole conditioning discussion."
    },
    {
      "volume": "CML",
      "volume_name": "Classical ML",
      "chapter": 2,
      "chapter_title": "Trees and Ensembles",
      "level": "L5",
      "type": "First Principles",
      "question": "Explain gradient boosting to someone who already understands gradient descent.",
      "answer_source": "Gradient descent updates a parameter vector by stepping against the gradient of the loss. Gradient boosting does the same thing where the \"parameter vector\" is **the model's predictions at the training points**—gradient descent in function space. Each round: (1) compute the negative gradient of the loss with respect to each prediction (the pseudo-residuals $r_i = -\\partial L / \\partial F(x_i)$); (2) you cannot apply that step directly—it is only defined at $n$ points and would not generalize—so **project it onto a function class** by fitting a small tree to the $r_i$ by least squares; (3) update $F \\leftarrow F + \\eta f$, with the learning rate $\\eta$ as the step size. \"Fit the residuals\" is what this reduces to under squared loss, where $r_i = y_i - F(x_i)$; under log loss the tree fits $y_i - p_i$; under a ranking loss it fits Lambda gradients [SR 5]. The tree is the descent direction; boosting rounds are iterations; early stopping is the convergence check against a validation set.",
      "red_flags_source": "- Presenting residual-fitting as the definition rather than the squared-loss special case.\n \n- No answer for why the raw gradient step must be replaced by a fitted tree (the generalization/projection point).\n \n- Confusing the learning rate on leaf values with the gradient-descent learning rate on parameters inside some tree (there is no such thing—trees are fit greedily, not by SGD).",
      "followups_source": "\"What are the pseudo-residuals for absolute loss, and what does that imply?\" \"Why does a lower learning rate generalize better?\" \"How does AdaBoost fit into this picture?\"",
      "status": "recut",
      "id": 132,
      "say": "Gradient boosting is gradient descent where the space you're descending in is functions, not parameters. In ordinary gradient descent you have a parameter vector, compute the gradient of the loss with respect to it, and step against it. In boosting, the object you're updating is the vector of the model's predictions at the training points: you compute the gradient of the loss with respect to each prediction and step against that.\n\nThe wrinkle is that the step is only defined at the n training points. You can't apply it directly - it says nothing about a new input and wouldn't generalize - so you project it onto a function class: fit a small tree to those negative gradients by least squares. The tree is your descent direction. Then update F to F plus eta times the tree, where eta is exactly a step size.\n\nWhich is why \"boosting fits the residuals\" is a special case, not a definition. Under squared loss the negative gradient happens to be y minus F of x, so it looks like residual fitting. Under log loss the tree fits label minus predicted probability. Under a ranking objective, lambda gradients.\n\nThen round it out: rounds are iterations, the learning rate is the step size, and early stopping on a validation set is your convergence check - because unlike gradient descent on a convex problem, more rounds eventually overfit by construction.",
      "numbers": [],
      "red_flags": [
        "Gives residual-fitting as the definition instead of the squared-loss special case.",
        "No answer for why you fit a tree to the gradient instead of taking the gradient step directly.",
        "Confuses the shrinkage learning rate with some learning rate inside the tree; trees are fit greedily, there is no SGD in there."
      ],
      "followups": [
        "What are the pseudo-residuals under absolute loss, and what does that imply?",
        "Why does a lower learning rate generalize better?",
        "How does AdaBoost fit into this picture?"
      ],
      "topic": "Classical ML"
    },
    {
      "volume": "CML",
      "volume_name": "Classical ML",
      "chapter": 3,
      "chapter_title": "Unsupervised Learning",
      "level": "L6",
      "type": "Mathematical",
      "question": "Derive the EM updates for a Gaussian mixture model, and explain why the likelihood increases monotonically.",
      "answer_source": "**Set up the obstacle.** The log-likelihood is $\\ell(\\theta) = \\sum_i \\log \\sum_k \\pi_k \\mathcal{N}(x_i \\given \\mu_k, \\Sigma_k)$: the log sits outside a sum, so nothing cancels and there is no closed form. Introduce latent one-hot $z_i$ with $p(z_{ik}=1) = \\pi_k$; the **complete-data** log-likelihood $\\sum_i\\sum_k z_{ik}[\\log\\pi_k + \\log\\mathcal{N}(x_i\\given\\mu_k,\\Sigma_k)]$ puts the log inside, and every parameter separates.\n\n**E step.** Posterior over the latent, by Bayes:\n\\begin{equation*}\n\\gamma_{ik} = \\frac{\\pi_k \\mathcal{N}(x_i \\given \\mu_k,\\Sigma_k)}{\\sum_j \\pi_j \\mathcal{N}(x_i \\given \\mu_j,\\Sigma_j)} .\n\\end{equation*}\nSince the complete-data log-likelihood is linear in $z_{ik}$, taking the expectation is just substituting $\\gamma_{ik}$, giving $Q(\\theta\\given\\theta^{(t)}) = \\sum_i\\sum_k \\gamma_{ik}[\\log\\pi_k + \\log\\mathcal{N}(x_i\\given\\mu_k,\\Sigma_k)]$.\n\n**M step.** With $N_k = \\sum_i \\gamma_{ik}$: setting $\\nabla_{\\mu_k}Q = \\sum_i \\gamma_{ik}\\Sigma_k^{-1}(x_i-\\mu_k) = 0$ gives $\\mu_k = \\frac{1}{N_k}\\sum_i \\gamma_{ik}x_i$; the analogous matrix derivative gives $\\Sigma_k = \\frac{1}{N_k}\\sum_i \\gamma_{ik}(x_i-\\mu_k)(x_i-\\mu_k)^\\top$; and $\\pi_k$ needs a Lagrange multiplier for $\\sum_k \\pi_k = 1$: $\\frac{N_k}{\\pi_k}+\\lambda = 0 \\Rightarrow \\pi_k = -N_k/\\lambda$, and summing gives $\\lambda = -n$, hence $\\pi_k = N_k/n$. Summarize: **every update is the ordinary MLE with hard counts replaced by soft responsibilities**.\n\n**Why it ascends.** For any $q(Z)$, $\\log p(X\\given\\theta) = \\mathcal{F}(q,\\theta) + \\KL(q \\| p(Z\\given X,\\theta))$ where $\\mathcal{F}$ is the ELBO; since $\\KL \\ge 0$, $\\mathcal{F}$ is a lower bound, tight iff $q$ is the posterior. The E step sets $q = p(Z\\given X,\\theta^{(t)})$, driving the KL to zero and making the bound **touch** the likelihood; the M step maximizes $\\mathcal{F}$ over $\\theta$, which can only raise it. Chaining: $\\log p(X\\given\\theta^{(t+1)}) \\ge \\mathcal{F}(q^{(t+1)},\\theta^{(t+1)}) \\ge \\mathcal{F}(q^{(t+1)},\\theta^{(t)}) = \\log p(X\\given\\theta^{(t)})$. Monotone ascent; bounded above for a regularized model; converges to a stationary point—local, not global. Close with the practical consequence: a **decreasing** log-likelihood in your training log is a bug, not a result.",
      "red_flags_source": "- \"EM alternates two steps until convergence\" with no ELBO or Jensen argument for why the likelihood rises.\n \n- Not being able to say why direct MLE fails (\"log of a sum\").\n \n- Dropping the Lagrange multiplier for $\\pi$, or claiming $\\pi_k = 1/K$.\n \n- Claiming EM finds the global maximum—or not knowing the GMM likelihood is unbounded above.",
      "followups_source": "\"What happens if a component collapses onto one point?\" \"How do you choose the number of components?\" \"Where does this become the VAE?\" \"What is the E step when the complete-data likelihood is not linear in $z$?\"",
      "status": "recut",
      "id": 137,
      "say": "You need EM because the log-likelihood has a log sitting outside a sum over components, so nothing separates and there's no closed form. Introduce a latent assignment variable per point and the complete-data log-likelihood puts the log inside the sum, so every parameter separates.\n\nThe E step is just Bayes. Given current parameters, compute the responsibility of component k for point i: the mixing weight times that component's density, normalized over components. Because the complete-data log-likelihood is linear in that indicator, taking its expectation is literally substituting the responsibility.\n\nThe M step is then the ordinary maximum-likelihood update with hard counts replaced by soft responsibilities. The mean is the responsibility-weighted average of the points, the covariance the weighted scatter about it, and the mixing weight the total responsibility mass over n, which needs a Lagrange multiplier because the weights sum to one. One sentence to remember: soft counts in place of hard counts.\n\nMonotonicity comes from the ELBO. For any distribution over the latents, the log-likelihood equals the ELBO plus a KL to the true posterior. The E step sets that distribution to the posterior, driving the KL to zero, so the bound touches the likelihood; the M step then raises the bound. Bound touched, bound raised - the likelihood cannot go down. Monotone ascent to a stationary point, local not global. Practical consequence: a decreasing log-likelihood in your training log is a bug.",
      "numbers": [],
      "red_flags": [
        "\"EM alternates two steps until convergence\" with no ELBO or Jensen argument for why the likelihood rises.",
        "Can't say why direct MLE fails - the log of a sum.",
        "Claims EM finds the global maximum, or doesn't know the unregularized GMM likelihood is unbounded above."
      ],
      "followups": [
        "What happens if a component collapses onto a single point?",
        "How do you choose the number of components?",
        "Where does this become the VAE?"
      ],
      "topic": "Classical ML",
      "whiteboard": "$\\gamma_{ik} = \\dfrac{\\pi_k\\,\\mathcal{N}(x_i \\mid \\mu_k, \\Sigma_k)}{\\sum_j \\pi_j\\,\\mathcal{N}(x_i \\mid \\mu_j, \\Sigma_j)}$ and, if they push on why it ascends, $\\log p(X \\mid \\theta) = \\mathcal{F}(q,\\theta) + \\mathrm{KL}\\!\\left(q \\,\\|\\, p(Z \\mid X, \\theta)\\right)$. Write the responsibility first and say every M-step formula is the plain MLE with gamma in place of a hard count; write the second line only when asked, and point at the KL going to zero as the moment the bound touches the likelihood."
    },
    {
      "volume": "CML",
      "volume_name": "Classical ML",
      "chapter": 4,
      "chapter_title": "Probabilistic Foundations",
      "level": "L5",
      "type": "Debugging",
      "question": "Training RMSE 0.31, validation RMSE 0.94, and the two learning curves are still far apart at 200K rows. Diagnose it and name the knobs, on the model of your choice.",
      "answer_source": "That gap with curves that have not met is the variance signature: the model is fitting sample-specific structure, and the still-converging curves say **more data will help**, which is the single most decision-relevant thing the plot tells you. Say what each term means first—bias is the error of the average model over training-set draws, variance is the spread across those draws, and $\\sigma^2$ is the noise floor—and note that bias and variance are properties of the **procedure**, not of the fitted object, because the expectation is over the training-set draw. The full identity and the deep-learning caveat live in [DL 2]; what matters here is which knob moves which term.\n\nThen work the knobs by model. **GBDT**: reduce rounds via early stopping (boosting overfits in $M$ by construction), lower the learning rate and raise rounds, cut depth or `num_leaves`, raise `min_child_weight`, add row/column subsampling, raise $\\lambda$. **Random forest**: this pattern is less common because averaging is already a variance killer, so suspect leakage or a broken split first; then raise `min_samples_leaf` and lower `max_features`. **Ridge/lasso**: raise $\\lambda$, and note that $\\lambda = \\sigma^2/\\tau^2$ means noisier targets want more of it. **kNN**: raise $k$—its variance is exactly $\\sigma^2/k$, the cleanest knob in classical ML. Across all of them: more rows, fewer features, more aggressive averaging.\n\nThen the seniority move—audit the measurement before spending a week on tuning. A gap this large invites two non-model explanations. **Leakage or a split defect**: a temporal split done randomly, group leakage where the same entity appears in both sides, or a target-derived feature will produce exactly this signature; check by comparing a random split against a temporal or grouped one. **Distribution shift between the two sets**: if validation is a later period, the gap may be drift rather than variance, and no regularization fixes drift. Only after those are cleared is \"tune for variance\" the right story.",
      "red_flags_source": "- Reciting the decomposition but unable to say what the expectation is over—the specific probe interviewers plant here.\n \n- Reading the numbers as high bias, or prescribing \"add capacity.\"\n \n- Jumping straight to hyperparameters without questioning the split.\n \n- Claiming the trade-off means you can never reduce both—more data, better features, and ensembling all do.",
      "followups_source": "\"How would you estimate the irreducible noise?\" \"The curves have already met—now what?\" \"Does this analysis change for a 7B-parameter network?\"",
      "status": "recut",
      "id": 142,
      "say": "That's the variance signature, and the most useful thing on that plot isn't the gap - it's that the curves still haven't met at 200 thousand rows, which tells you more data will help. That's the decision-relevant fact.\n\nBefore I touch a hyperparameter, though, I'd audit the split, because a gap that large has two non-model explanations that no amount of regularization fixes. Leakage or a broken split: a temporal problem split randomly, group leakage where the same user or merchant lands on both sides, or a feature derived from the target. Compare a random split against a grouped or temporal one - if the gap moves, that's your answer. And distribution shift: if validation is a later period, you may be looking at drift rather than variance, and regularization won't touch drift.\n\nOnce that's clear, the knobs depend on the model. GBDT: early stopping on rounds first, because boosting overfits in the number of rounds by construction, then a lower learning rate with more rounds, shallower trees, higher min_child_weight, row and column subsampling, more L2. Random forest: this pattern is unusual, since averaging already kills variance, so suspect leakage before tuning, then raise min_samples_leaf and lower max_features. Ridge or lasso: raise lambda. kNN: raise k, whose variance is exactly sigma squared over k - the cleanest knob in classical ML.\n\nAnd across all of them the same three levers: more rows, fewer features, more averaging.",
      "numbers": [
        "kNN variance is sigma^2 / k, so k is the cleanest variance knob available"
      ],
      "red_flags": [
        "Reads a train-validation gap this size as high bias and proposes adding capacity.",
        "Jumps straight to hyperparameters without questioning the split.",
        "Recites the bias-variance decomposition but can't say the expectation is over the draw of the training set."
      ],
      "followups": [
        "How would you estimate the irreducible noise?",
        "The curves have already met - now what?",
        "Does any of this change for a 7B-parameter network?"
      ],
      "topic": "Classical ML"
    },
    {
      "volume": "CML",
      "volume_name": "Classical ML",
      "chapter": 5,
      "chapter_title": "The Applied Craft",
      "level": "L5",
      "type": "Conceptual",
      "question": "When is random k-fold cross-validation invalid, and what do you use instead?",
      "answer_source": "Random $k$-fold assumes the rows are exchangeable and that the validation set stands in the same relationship to the training set as production will. Three situations break that.\n\n**Time structure.** If the data is time-ordered or the deployment is \"train on the past, score the future,\" shuffled folds train on the future to predict the past and share period-level structure (a promotion, an outage, a fraud campaign) across the split. Use rolling-origin or expanding-window splits, and when labels have a maturation window—a 30-day churn label—add purging (drop training rows whose label window overlaps validation) and an embargo gap.\n\n**Repeated entities.** If a user, patient, merchant, session, or query contributes many rows, random folds put the same entity on both sides, and the model is credited for memorizing identity. Use `GroupKFold`, or split on a hash of the entity ID; `StratifiedGroupKFold` when the classes are also imbalanced. Duplicates and near-duplicates are the same failure with a different key, so deduplicate before splitting.\n\n**Small or imbalanced classification** does not invalidate random folds, but unstratified folds can contain zero positives—use stratification.\n\nThe generative rule behind all of it: **the split must simulate the deployment gap.** Write down what production will actually ask—new entity or known entity, future period or same period—and construct the split so validation stands in that relationship to training. That rule also tells you when grouping is **wrong**: if the model will always score users it has already seen, a user-disjoint split understates production performance and will make you reject a good model. And when several conditions apply at once, the temporal axis usually dominates.",
      "red_flags_source": "- Reciting \"use TimeSeriesSplit for time series\" with no notion of why, or applying grouping reflexively without asking what production scores.\n \n- Missing group leakage entirely—the failure that has invalidated published results.\n \n- Believing stratification fixes temporal or group problems.",
      "followups_source": "\"Your labels mature over 30 days—exactly how does that change the split?\" \"The model scores both existing and brand-new users. Design the evaluation.\" \"How would you detect group leakage if you did not know the group key?\"",
      "status": "recut",
      "id": 147,
      "say": "Random k-fold assumes rows are exchangeable and that validation stands in the same relationship to training that production will. Three things break that.\n\nTime structure. If the data is time-ordered, or deployment means train on the past and score the future, shuffled folds train on the future to predict the past and share period-level structure - a promotion, an outage, a fraud campaign - across the split. Use rolling-origin or expanding-window splits, and if labels mature, like a thirty-day churn label, add purging: drop training rows whose label window overlaps validation, plus an embargo gap.\n\nRepeated entities. If one user, patient, merchant or query contributes many rows, random folds put the same entity on both sides and you're crediting the model for memorizing identity. Use GroupKFold, or split on a hash of the entity ID, and StratifiedGroupKFold when classes are imbalanced too. Near-duplicate rows are the same failure with a different key, so deduplicate before splitting.\n\nSmall or imbalanced classification doesn't invalidate random folds, but unstratified folds can end up with zero positives - so stratify.\n\nThe rule underneath all of it: the split must simulate the deployment gap. Write down what production will actually ask - new entity or known one, future period or the same - and build the split to match. That also tells you when grouping is wrong: if you'll only ever score users you've already seen, a user-disjoint split understates production performance and makes you reject a good model.",
      "numbers": [],
      "red_flags": [
        "Recites \"use TimeSeriesSplit for time series\" with no notion of why, or applies grouping reflexively without asking what production scores.",
        "Misses group leakage entirely - the failure that has invalidated published results.",
        "Believes stratification fixes temporal or group problems."
      ],
      "followups": [
        "Your labels mature over thirty days - exactly how does that change the split?",
        "The model scores both existing and brand-new users. Design the evaluation.",
        "How would you detect group leakage if you didn't know the group key?"
      ],
      "topic": "Evaluation"
    }
  ]
}
