# Conventional ML Essentials

Volume IV of the ML Interview Program: classical machine learning for Staff-level
breadth, depth, and coding rounds — linear models through gradient-boosting
internals, unsupervised learning, probabilistic foundations, the applied craft
(leakage, CV, interpretability), experimentation & causal inference, time series,
and the from-scratch coding canon with grading rubrics.

Status: scaffolded (Phase A). `docs/coverage-spec.md` is the ideal-coverage spec
and the diff checklist that the author's own conventional-ML document is mapped
against on arrival (Phase C3). The two treaty-critical chapters (Trees/GBM,
Experimentation) are written first regardless.

Build: `tectonic main.tex` (style synced from `ml-interview-program`).
